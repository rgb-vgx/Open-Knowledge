# 152. Athena, Glue, and S3 Data Lakes - Hands On
# Section: Analytics
# https://ua.udemy.com/course/aws-data-engineer/learn/lecture/40413292
# Caption: en_US (auto)

All right, let's, uh, create a little fake data lake here, if we will.
And we're actually going to reuse the same data that we used for our magazine reviews from Amazon back
in the database section.
So if you have not already done that exercise, you might want to go back to the database section and
do the exercise for using redshift, at least up to the point where we set up our S3 bucket.
Or you could just head on over to the course materials site here at Sundog Education.com slash AWS Data
Engineer.
There is a link here to the course materials, and the short version is you just want to create an S3
bucket with your own unique name that contains the metadata and reviews folders that are underneath
the magazine reviews data within that course materials.
For this activity, we're just going to use the reviews themselves.
So this is a JSON file of magazine subscription reviews from Amazon.com.
And back in the earlier exercise we went through that structure and what's in there.
So again, I would encourage you to go back and do that exercise for redshift first.
If you haven't already.
All right.
So if you have that data in place in S3, what we're going to do in this exercise is use glue to extract
some structure from that semi-structured data that's in S3, and create a schema that we can use to
query that data as if it were a SQL database from Amazon Athena in place.
So what's different is that when we use this in in redshift earlier, we copied this data into redshift.
This time we're just going to be querying it in place while it's in S3 as a data lake instead.
So you could imagine a world where there's more than just one JSON file in here.
Maybe we had many JSON files for different categories, and maybe we have many JSON files for reviews
made at different times.
Right?
So you could imagine this is a much larger repository of review information and not just a single file.
So with that, let's go to glue and try to extract some structure from this semi-structured data.
I can just search for glue here AWS glue.
And let's go ahead and create a new database This is basically a virtual database.
It's going to look like a database to Athena and other tools, but it's not copying data at all.
It's just giving us a schema on top of our data lake in S3.
So let's add a database.
Let's give it a name, let's call it magazines, and you can give it a description if you want to.
But I'm lazy so I'm not going to type that out.
Create database.
And now what we can do is go to tables and create a new table for those reviews using a crawler.
So we're going to set up a glue crawler to take a look at that JSON data and try to infer its schema.
This crawler needs a name, let's call it I don't know.
Um magazine review crawler.
Again, you can give it a description if you want to, but I'm lazy.
Our data is not mapped to a glue table.
That's the point.
We want to make a new one, so we'll keep that on.
Not yet.
And add a data source from S3.
So we don't need a fancy network connection.
It is in this account.
Let's go ahead and browse S3 for the folder that this lives in.
So be careful not to select the file itself.
That won't work.
It'll look like it worked, but it just won't work when you try to query it.
So very subtle edge case there.
So what I want to do is select the reviews folder here and not the actual file inside of reviews.
So choose that.
Make sure it has a slash at the end and it's all happy, even though that was read at first.
If I just click outside of that, it accepts that and it's a valid path.
Now, if we did have subfolders, we could say how to deal with that.
So often in a data lake environment, things might be partitioned, right?
You might have a folder for each year, and each year might have a sub folder for each day.
Each day might have a sub folder for each hour, and so on and so forth.
In this case, we only have one level, so it's not relevant.
But in a real world scenario with a larger data lake, that would be something to think about.
Let's go ahead and add our S3 data source and click on next.
We need to give it an IAM role.
So let's create a new one here.
It makes it very easy.
Just give it a suffix.
I don't know, magazines, whatever you want.
And that will automatically give it the permissions that it needs.
We're not using lake formation here, so nothing to do there.
And we're not doing anything special with the at rest encryption for CloudWatch logs.
So we'll hit next.
Target database.
Let's select the magazine's one we just made.
And we don't need a prefix.
But if you wanted to have a table name prefix to sort things better, you can do that.
Also, if you run into a situation where the cruel crawler is just generating tons and tons of tables,
sometimes it gets confused about partition.
So this can be a way to sort of debug that and set that to a limit, an upper limit of how many tables
it can create.
Sometimes if it doesn't infer their partition structure properly, it will create different tables for
each partition instead of putting everything under one table.
So that's what that is there for.
I'm going to say to run this on demand now again in the real world, you might run this on a schedule
to actually pick up new data automatically or pick up changes in your schema automatically.
Let's talk about that, by the way under Advanced Options, there is some options on what to do if the
schema changes.
So you might remember way back in the Data Engineering Fundamentals section we talked about the concept
of schema evolution.
That's what this is about.
You're telling it how to handle it.
If the schema changes you can say whatever changed.
Go ahead and upload the table definition to match it.
Or maybe only add new columns that you see and don't actually delete old ones.
That might make for better compatibility with existing queries, but we're going to keep that as is,
because my scheme is not going to change, and I'm only going to run it on demand because I don't want
to incur any unexpected costs if I forget to delete this crawler when I'm done.
So let's hit next and review all the settings and go ahead and create crawler.
I'm not done yet though.
I need to run it.
Run crawler.
It is attempting to start.
It should take about a minute for that to finish.
All right we're now in running status.
And, uh, I could just come back when that's done, but I think it's only going to take another 20s
or so here.
All right.
Took a little bit longer than I expected, but it finished after about a minute, so that is now in
completed status.
However, sometimes that lies, sometimes it's not really done writing everything out.
So let's go back to the crawlers selection here.
And you can see that it's actually not quite done.
It's still in stopping status here.
So let's wait for that to show.
Ready and succeeded for the last run because it's not actually done writing out that schema quite yet.
Uh, another few seconds and it'll be done.
Eigtht.
All right.
That actually took several minutes.
You never know what you're going to get with AWS, do you?
But it did say ready now.
So let's go ahead and see what it did.
Let's go back to our databases and select the magazine's database we created.
And now there is a reviews table.
Yay.
Let's go ahead and take a look at that and let's see what it extracted.
So this is the schema that it came up with.
Now this schema is valid.
That is based on the JSON data that I found.
However, things like structures, nested structures and arrays, those don't really map well to columns
in a relational database.
Right?
So in a real ETL pipeline, if I cared about that information, I would have to transform that in some
manner and change that into something that might be appropriate for a SQL query.
If that was my end goal to actually query these fields using SQL.
However, my task at hand here does not require that information at all.
All I really want to do is find who the top reviewers are.
That's that's our end game here.
And to do that, I don't need the images associated with this item or any images associated with the
review.
Rather, nor do I need this complicated structure of the style of the review itself.
So I'm just going to delete those.
Let's hit edit schema.
Select those two struct and array data types, which will not map well to Athena and just delete them.
If I were to try to query this as it is, it would give me an error and well let's just try to head
that off So now I can save the schema as a new table version.
Hey, my schema evolved already and these all look like valid data types that can work well in a relational
database SQL kind of setting.
All right.
So let's go back to our tables view here.
And I need to refresh it.
There's my reviews table.
And you can see here there's a handy link for table data.
All this does is take you to Athena.
So I could either click this link or go to Athena from the console to query this data.
But I'm lazy, so let's click the link.
This warns me that I'm going out of glue and into Athena, and Athena has its own costs associated with
it.
But don't worry, this won't cost much at all.
And it very conveniently gave me a pre-generated query to just sort of sample that first ten rows of
that data and look, we have data.
Cool.
Wow.
Now, if this is your first time using Athena, you might have got an error about the output destination
or on things that no worries, just go to the settings tab up here, and from there you should be able
to manage the query result location.
You just have to tell it where to put its results in S3.
So click on manage and from there you can navigate to any S3 bucket that you might have or go create
one if you need to and just hit save.
And that's all you need to do.
And after that, you can go back to the query editor and run that query again.
Awesome.
So overall again is the review score 1 to 5 stars.
We have a unique reviewer IDs and reviewer names and the Asins that identify the actual magazine they're
reviewing.
So very cool.
Let's do something here.
Let's do a query.
So here's a little challenge for you to stretch your SQL muscles.
Write a query that identifies the top reviewers, and I'll let you have a go at that.
Go ahead and hit pause if you want to tackle that yourself and come back when you've finished that and
you can compare it to my solution, hit pause now if you want to do that.
Otherwise I will just paste in what worked for me All right.
so what's going on here?
Let's go ahead and run this.
First of all make sure it really works.
Check it out.
So our top reviewer was Brian Carey with 55 reviews, followed by Dale DK with 26 reviews.
Hey, isn't DK a publisher?
They better not be reviewing their own magazines.
Hmm hmm.
Anyway, so here's what that query looks like.
We're going to group everything by reviewer ID and reviewer name.
Now we're doing that because if you leave out the reviewer ID and just group it on reviewer name, there
are some reviewers that have the same name.
So, you know, Jane, for example, might come up under multiple different reviewer IDs or Mike or
Mark, right?
A lot of them just go by their first name.
So that's not actually a unique identifier for a reviewer, but the reviewer ID is.
So by nesting those together by grouping on this nested reviewer ID then reviewer name.
I can see when that happens, and there aren't any really examples of that happening in my results here,
where a given reviewer ID in these top results maps to more than one name.
But this actually gives you better results.
If I were to group by just reviewer name and not reviewer ID, I would get a very different set of results
because of that problem.
And if I were to just group on reviewer id instead of reviewer name, well, I wouldn't have any names
and I wouldn't know who these people are.
So that's why we did it that way.
We have that nested grouping going on here.
So we're going to group by reviewer ID and reviewer name order by the review count, because that's
what I'm interested in.
Who are the most prolific reviewers in descending order.
And for what I'm displaying, we're going to select the reviewer ID, the reviewer name, and the count
of reviews as review count.
So there you have it Athena in action querying data in place in S3 and in S3 data Lake using a schema
that I built using a glue crawler.
Now it is actually possible to load data from S3 within Athena directly.
You could say create and create it from S3 bucket data without involving glue directly.
So that's a little shortcut there.
If you just want to use Athena.
But by having that schema in the glue data catalog that allows me to query that data using other tools,
potentially.
So it's a good thing to have.
So there you have it.
Athena S3 glue, data crawler glue data catalog.
All in action there.
Let's clean up our mess, shall we?
Nothing to clean up in Athena, per se, but we do want to blow away those glue resources.
So let's go back to the glue console and go ahead and delete that magazine's database.
Yes I'm sure.
Let's delete the crawler as well just to be safe.
Action delete crawler.
Make sure that took.
Yep.
And what's left?
IAM roles don't really cost anything.
I'll just leave that.
But let's let's get rid of that S3 bucket if you're done with it as well.
That won't cost you much, but it costs something.
So that was mine.
DCO one Sundog yours is called something else.
Let's delete it.
Oh, it has to be empty first, so not quite that simple.
First we have to delete everything inside of it.
Delete.
Permanently.
Delete.
And also under metadata, we'll delete that file as well.
This is a lot easier using third party tools.
And now we should be able to delete that entire bucket.
Since it is now empty.
Delete.
And I need to type in the bucket name to confirm that.
Yes I want to get rid of that data.
All right.
We have successfully cleaned up our mess and successfully illustrated the use of a data lake and querying
that using Athena and glue.
Very cool.
