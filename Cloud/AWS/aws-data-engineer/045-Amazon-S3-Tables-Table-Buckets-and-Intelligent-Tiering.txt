# 45. Amazon S3 Tables: Table Buckets and Intelligent Tiering
# Section: Storage
# https://ua.udemy.com/course/aws-data-engineer/learn/lecture/54715823
# Caption: en_US (auto)

[Instructor] Let's talk about some features of S3 Tables.
They're pretty easy to set up.
There's not a whole lot to think about,
but under the hood, there's a lot going on.
S3 Tables are purpose-built for tabular data,
so if you're doing data and analysis of structured data,
S3 Tables is gonna be a lot more efficient
than this throwing raw CSV data into an S3 bucket.
So, it's not just this, you know, data swamp, if you will,
of just unorganized data.
It's a more structured, purpose-built home for that data,
and it promises up to three times faster performance
and up to 10 times more transaction rates
compared to just throwing CSV data
into a general-purpose S3 bucket.
This does, however, come at a cost.
It is more expensive than just throwing your data into S3,
but the trade-off is you get better performance
and better scalability.
The other thing you get is managed table optimization,
so it is automatically going to deal
with things like data compaction for you.
So, as you add more and more data,
it will compress and compact that data
to make it more efficient over time
without you doing anything.
It will also manage snapshots for you automatically,
and it will also deal with unreferenced file removal
for you automatically as well.
So, you can set it up so if things aren't referenced
or used at all within a certain number of days,
it will just automatically remove that data to save space.
Also, it has full integration with IAM,
Identity and Access Management,
and also service control policies, or SCPs,
through Amazon Organizations for security purposes.
And do remember, though, that it is a distinct namespace,
so S3 Tables are their own thing separate from S3.
So, you'll use the service namespace s3tables
when referring to permissions for different operations
on S3 Tables, not S3.
The basic building block of S3 Tables is the table bucket,
so every table bucket will contain your data
in this structure that you see to the right here.
And again, these two are gonna be
under the s3tables namespace,
so the ARNs for these things
are going to be arn:aws:s3tables:
colon whatever your region is,
whatever your account is, and then your bucket name, okay?
Now, the organization of these is important.
So, a table bucket is a very large, high-level thing,
and this is basically where you're going
to be organizing the properties
that you want for your table bucket.
You know, what are its policies for encryption
and for data management and whatnot.
Within that table bucket,
you can have multiple namespaces,
and a namespace in an S3 table bucket
is analogous to a database in a lot of other systems,
so usually a namespace and a table bucket
will be referred to as a database in other analytics tools,
so that's the way to think about it.
And like you normally think of databases,
a database can contain multiple tables.
So, the idea here is a namespace will have some sort
of a logical connection between the tables within it, right?
So you might have a namespace for some higher-level concept
and individual tables that might refer
to each other within that namespace.
So, that's the structure.
Table bucket at the top,
namespaces within that bucket,
tables within the namespaces, okay?
There's two types of table buckets out there.
There's customer-managed ones,
the ones that you're creating there yourself,
and there's also some AWS-managed ones as well,
so things like metadata and inventory tables
are gonna be managed under the hood for you automatically.
You generally don't have to think about those.
They just exist,
and well, they're gonna use up some reserved names
that you need to be careful of as well.
Your names, by the way, for your table buckets
only need to be unique within your account,
so it's not like a general-purpose S3 bucket
that has to be globally unique, thankfully.
It only has to be unique within your own account, so yay.
There are some important restrictions, though,
that aren't totally well-documented.
It must be lowercase for one thing.
If they are not all lowercase,
you won't be able to integrate your table bucket
with Lake Formation or Glue or other analytic services,
so very important there.
You'll get a very strange, cryptic error
if you put an uppercase letter in your table bucket,
so watch out for that.
And that same restriction exists on namespace names as well.
Those also should be lowercase.
And once you make them, they can't be changed.
You can also attach IAM policies to a table bucket
and use that to restrict access to creating new namespaces,
creating new tables, reading those tables,
whatever you wanna do.
So, IAM, like any other Amazon service,
is gonna be your primary line of defense
for security and access control.
You can also attach tags to your table buckets
as well if you want to,
just like any other S3 bucket.
For managing the actual storage,
they have Intelligent-Tiering available
in S3 Tables as well, and there are three tiers.
This is used to optimize your storage costs, of course.
So, if you have less frequently used data,
you can store that in a lower tier to save some money
based on the access frequency.
By default, everything will be Frequent Access.
This is also referred to as Standard Access in the UI.
So, by default, any new table will be Frequent Access,
and accessing a table will move it back
to the Frequent Access tier.
Now, compaction only happens in Frequent Access,
so if you do have data that bumps down to lower tiers,
they will not be automatically compacted for you.
Now, if your data remains untouched for 30 days,
they will get bumped down to Infrequent Access
using Intelligent-Tiering if you've enabled that,
and that will be a little bit cheaper.
And even the cheapest option,
after 90 days, if your data is untouched,
that will get bumped down to Archive Instant Access tier
when S3 Tables Intelligent-Tiering is enabled.
This is just an option that you'll see
when you're setting up your S3 table bucket.
And of course, all of these tiers offer 99.9% availability
and a whole bunch of nines in durability.
