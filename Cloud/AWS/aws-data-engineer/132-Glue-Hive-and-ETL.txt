# 132. Glue, Hive, and ETL
# Section: Analytics
# https://ua.udemy.com/course/aws-data-engineer/learn/lecture/40356198
# Caption: en_US (auto)

So we haven't talked about Apache Hive yet,
but basically Hive is a service
that you might be running on Elastic MapReduce
that allows you to issue SQL-like queries on data
that's accessible to your EMR cluster.
I say SQL-like because technically it's called HiveQL,
but the differences between HiveQL and SQL
really aren't important for the purpose of the exam.
What is important though is that you know
that Glue can integrate with Hive.
So, you can use the AWS Glue Data Catalog
that was extracted by your Glue Crawler
as your metadata store for Hive.
In Hive, that's called a metastore.
That's sort of special Hive terminology.
Conversely, you can also import a Hive metastore into Glue.
So if you have an existing Hive metastore
or Hive database sitting in EMR,
you can turn around and make a Glue Data Catalog
out of that Hive metastore as well.
You can go both ways.
Now this will make a little bit more sense
when we talk more about Hive in more depth later on,
but just remember that the Glue Data Catalog
can provide meta information to Hive running on EMR.
So, just like Glue can provide the glue
between your unstructured data on S3
and Amazon services like Redshift and Athena,
it can also provide the glue between services running
on your EMR cluster, you know,
more traditional open-source applications
such as Apache Hive.
Let's talk a little bit more in depth about Glue ETL.
Again, this is very important for the exam.
Not a lot you need to know, but you need to know it.
So, make sure you internalize this stuff, folks.
So, Glue ETL will automatically
or can automatically generate code for you
for transforming your data.
So, you can go to the the Amazon console
and say how you wanna transform your data
in a graphical manner
and it will automatically generate code for you
that will be running under Scala or Python
and taking advantage of that Apache Spark engine
that's running under the hood for Glue ETL.
So, you can also write your own code if you want to,
using Scala or Python.
It will handle encryption.
So, you can have server-side encryption at rest
or SSL encryption for handling data that's in transit.
Those are both handled for you.
Glue ETL can be event-driven.
So you can have ETL processes that kick off automatically
as soon as new data is seen by Glue.
Also, something you might need to do
is provisioning additional DPUs.
That's a data processing unit to increase
the performance of the underlying Spark jobs.
So, even though Glue bills itself as being fully managed,
under the hood, there are still servers
that need to run these Spark jobs
that are running the ETL processes for you.
And if you find that it's taking too long,
you can provision additional DPUs
to give that Spark cluster
that's running under the hood
a little bit more capacity to work with.
How do you know how many DPUs you need?
Well, you can enable job metrics in your Glue ETL job
to help understand the maximum capacity that you do need.
So, the process for figuring out how many DPUs
you need to set for your cluster is to enable job metrics,
study those, and then set the maximum capacity
for DPUs in your job ahead of time.
So, that's the process you follow for tuning
the performance of your Glue ETL job.
To be more specific, you'll visualize those metrics
in the Glue console as opposed to CloudWatch,
and some things you can plot would be
the maximum needed executors
versus the maximum allocated executors
to see if you have not enough there to go around.
And once you have that information,
you can figure out how many DPUs you need
to fulfill the number of executors that you actually need.
So, remember, a DPU includes two executors,
but you do need to reserve one DPU for the Spark driver
and one executor for management overhead.
So, taking that into account,
you can arrive at the appropriate number of DPUs
for a given job.
You can also plot ETL data movement
to make sure that you have enough data capacity
moving around as well there, too.
So again, you can plot these metrics in the Glue console
and use that information to tune the number of DPUs
you have allocated to your job.
If there are errors encountered along the Glue ETL pipeline,
you can have those reported to CloudWatch automatically.
And if you want to be notified of those,
you could even tie a CloudWatch into SNS
to make sure that you get notified, or you know,
get a text message or whatever it is
if your ETL process runs into trouble.
So again, I wanna make sure you understand this,
Glue ETL is a system that lets you automatically process
and transform your data.
You can do this by a graphical interface
that lets you define how you want
that transformation to work.
And it will actually do that using Apache Spark
under the hood using Scala or Python code,
fully encrypted, both at rest and in transit.
Can be event-driven if you want it to be
or it can run on a schedule.
A little bit more about Glue ETL.
So again, just wanna reinforce this,
it can be used for transforming your data,
cleaning your data,
or enriching your data before doing analysis.
It will generate that ETL code in Python or Scala
and you can actually modify that code
if you need to once it's been generated.
So, if the generated code isn't doing exactly what you want,
you can just go in and modify that code
to tune it and do exactly what you want.
So, you can use the automatically generated code
as sort of a starting point and build on top of that.
That's a really great way to do
more complicated operations using Glue ETL.
You can also provide your own code
if you want to hand it your own Spark or PySpark scripts.
So, a Spark script might be ready in Scala,
but if you prefer Python, which a lot of people
in the data analytics world do,
you can just provide a Python script
for Spark called PySpark to Glue ETL to work with
and it will just hand that off
to the underlying Apache Spark engine
for transforming that data, cleaning it, enriching it,
whatever you need to do to process that data
before you actually analyze it using later systems.
The target of Glue ETL can be S3,
or it can be talking through a JDBC interface,
to some other database, including RDS or Redshift,
or it can output data into the Glue Data Catalog.
So, you could have an ETL job
that just generates a data catalog as well.
Again, it is fully managed,
although you do have some control
over the underlying resources,
how many DPUs you have, like we talked about.
It's very cost effective, and again,
with most of these serverless managed systems,
you only pay for the resources that you actually consume.
The jobs are run on a serverless Spark platform,
and we say serverless, yes, there are servers under the hood
actually running that Spark code,
but you don't see them, you don't have to manage them.
So, that's why we call it serverless.
It's all kind of magic.
There is a Glue Scheduler you can use to schedule those jobs
or you can use Glue Triggers
to automatically run jobs based on whatever events you want.
So, please take a moment to internalize this slide
and the information on it.
Very important stuff to understand for the exam.
So, how do you go about applying
these ETL transforms in Glue?
Well, the main data structure
that you'll be interacting with is called the DynamicFrame.
And if you're familiar with Apache Spark,
this is gonna feel very familiar.
It's very much like a Spark DataFrame object,
the only difference is that it has more ETL stuff
available to you within that class.
It has Scala and Python APIs available, whatever you prefer.
And very much like a DataFrame
is a collection of rows in Spark,
a DynamicFrame is a collection of DynamicRecords
where DynamicRecords are these self-describing records
that have some sort of schema associated with them.
So, you can see in this diagram on the right here,
you can think of the DynamicFrame
as encompassing a collection of DynamicRecords,
and each DynamicRecord contains fields, columns,
whatever you wanna call them, within that record.
In this case ID, year, month,
but could be anything, right?
And you can have as many DynamicRecords as you want.
Those will all be stored in a distributed manner,
probably on S3 under the hood, right?
So, just to make it real, here's a few code snippets here.
Remember that you'll never be asked
to write code on the exam itself,
but if you are a coder,
this might help make it more concrete for you.
For example, if you wanted to create a DynamicFrame
called pushdownEvents,
we might just say glueContext.getCatalogSource
and load that from some database table somewhere.
And then to apply a mapping to that DynamicFrame,
we could just call pushdownEvents.applyMapping
and what that will do is apply that mapping
to every single record within that DynamicFrame.
Of course, in a distributed manner.
That's the whole point, right?
So, in a specific example, we're defining a mapping
that converts the IDs from strings to longs.
It's renaming the actor.login field to just actor,
repo.name to just repo, so on and so forth.
The details aren't terribly important
but it kind of drives home the concept here,
you can create a DynamicFrame
and in just a couple of lines of code here
we can apply some complex mapping
to every single record in our DynamicFrame
in a distributed manner.
Let's talk a little bit more about the transformations
you can do with Glue ETL.
There are certain bundled transformations
that come with it out of the box
that includes drop fields or drop null fields.
So, if you just want to remove empty data,
which is a very common operation in pre-processing,
it can do that for you with just a couple of clicks.
So, it can automatically drop fields
or drop null fields to remove empty data
from your incoming data for you.
You can also do filters where you specify
your own function for filtering records.
So, if you do need to specify some criteria
where you want to throw out outliers
or data that might not fall within the bounds
that you expect, you can set up these filters
to filter records out based on that criteria.
Or maybe you just want to extract
only a subset of your data to analyze.
That's another valid use of filters, right?
You might wanna say,
I only want to filter data from the US for this job,
or I only want data from the UK, whatever it might be.
A filter transformation will let you do that
and that is a bundled operation
that's very easy to do with Glue ETL.
You can also do joins.
That's also a bundled transformation
that's provided as part of Glue ETL.
So, if you need to enrich your data by joining it
with other tables, you can do that.
You know, a common operation there is taking some code,
or like a country code or something,
and enriching that with the full name of that country.
I mean, I don't wanna get into database theory too much,
but joins are generally used to enrich data.
For example, I use the movie lens dataset
a lot in my courses, where you might have a movie ID.
If you want to actually put the movie name
into the output data, you might do a join
with a separate table that maps movie IDs to movie names,
as an example.
You can also do maps,
and this allows you to do things
like add fields to your data, delete fields,
or perform external lookups manually.
So, if you just need to transform your data
one row at a time, maybe that just includes,
you know, selecting which fields that you want
to pass on to the final analysis.
That's a very common operation.
You can use a map operation to do that.
So, for example, if there's a bunch of columns of data
coming into your data, you can use a Map operation
in Glue ETL to extract the fields
that you actually care about
and delete the ones that you don't care about.
There are also bundled machine learning transformations.
That's pretty cool stuff.
One that you'll probably see mentioned on the exam
is the FindMatches ML transformation,
and its purpose in life is to identify duplicate
or matching records in your dataset,
even if those records do not have a common unique identifier
and no fields match exactly.
So it can actually learn what a duplicate record looks like,
even if it's not really a duplicate.
You know, for example,
if you have duplicate addresses that vary
by some subtle thing, like somebody spelt out street
instead of using the abbreviation,
you might use FindMatches ML to handle that.
So, that's a tool that's available for you,
just remember it exists and that's what it does.
You can also use Glue ETL for format conversions,
and some supported formats that it can automatically convert
between are CSV, JSON, Avro, Parquet, ORC, and XML.
So, a lot of times you'll have data coming in as one format
and you need to transform it into something else
for a downstream system,
Glue ETL can do that for you very easily.
And also, since again, it's using Apache Spark
under the hood, anything Apache Spark can do,
you can do with Glue ETL.
That includes even fancy things like K-means clustering,
you know, that's coming from Spark's ML lib package.
We'll talk more about Spark later in the course,
but just remember anything Spark can do, you can do in ETL.
So, again, do take a moment to pay attention
to this slide, guys.
It is important to understand Glue ETL in depth
and what it can do.
One more method on the Glue DynamicFrame
that I wanna talk about is ResolveChoice.
I'm hearing this is actually showing up on the exam now,
so we should talk about it.
It's purpose in life is to deal
with ambiguities within your DynamicFrame
and return a new one that does not have ambiguities.
What do I mean by an ambiguity?
Well, for example, if I have two fields
that have the same name within my DataFrame,
that would be an ambiguity that we need to resolve somehow.
And there are four ways right now
that you can deal with those ambiguities.
One is you could say make_cols as a parameter
to the ResolveChoice API.
And what that will do is create a new column
for each unique type that that name appears under.
So, look down here at the bottom,
we have this example here where we have
a myList DynamicFrame that contains two fields called price.
One of them is a numeric double-position number,
the other one is a string
that contains the dollar sign symbol.
So if we were to call make_cols on this example,
we end up with two new columns,
one called price_double and one called price_string
to resolve that ambiguity.
Another option is just cast
and that will force every instance
to be cast to one specific type.
We can also say make_struct,
which creates a structure within your record
where it contains each data type individually
within that structure for price.
So, we might have a new structure there called price,
inside that price structure would be the decimal version
and the string version of that field name.
And lastly, we have project,
and that will just force things
to a given type that you specify.
So, if I were to say projectString,
it would just throw away any data types
that are not a string.
So, that price of 100.00 would just go bye-bye.
We'd be left with a string version of that instead.
So that's what ResolveChoice is all about.
It's just about resolving ambiguities
where you have different columns
with the same name within your DynamicFrame.
