# 247. AWS CloudTrail Lake
# Section: Management and Governance
# https://ua.udemy.com/course/aws-data-engineer/learn/lecture/40394664
# Caption: en_US (auto)

Tutor: A newer feature in CloudTrail
is called CloudTrail Lake,
and this is a managed data lake for Cloud Trail events.
So it's going to automatically take care of storing
all of that Cloud Trail data into S3 somewhere,
and integrating the collection, the storage, the preparation
and optimization for all of that data
for subsequent analysis and queries.
It does that by converting all of the events
into ORC format, which is a columnar data format,
that makes it quicker and more efficient to search
within a data lake environment
when your queries are only interested in certain columns,
which is usually the case.
So this allows you to query your CloudTrail data
just using SQL, which is pretty darn neat,
and we'll see an example of that in a few slides.
But that's the whole point.
By putting all of this Cloud Trail data into a data lake,
we can query it just like a data lake
and make it easy to extract insights
from all that Cloud Trail data.
To turn it on, all you have to do
is click the create event data store menu choice
in the console when you're setting up Cloud Trail,
and you'll see a screen like this to the right.
You just have to give it a name and a retention period.
By default, the data retention period is up to seven years.
Once you do that, you'll have a few more choices to make.
You need to specify the event types you want to track.
You can specify logging management events
and or data events.
So if you're not interested in logging
and us paying for the storage
and indexing of all of the resource operations,
you wanna leave data events off,
if all you're caring about is keeping track
of who's doing what within your services
and you just want management events.
However, within management events,
that also includes KMS events,
and those KMS events can add up very fast,
because you hit KMS a lot
whenever you're doing anything,
if anything's secure, if you're using KMS at all.
So that can make your costs blow up really quickly.
So remember, CloudTrail can generate a lot of data
and you want to be picky about what sorts of data
you're actually storing in Cloud Trail Lake
to keep your costs under control, right?
So if you don't need,
if you don't care about all the KMS requests,
don't turn that on.
Exclude KMS events, that's off by default you'll see,
for a good reason.
Basic event selectors can be selected in the UI.
You can see here that we can also choose
RDS data API events and stuff like that.
But if you need finer grain control
over what's being collected, you can do that
with what's called advanced event selectors.
And that can further help you control your storage costs
and control what sort of data you're ingesting.
You can also create what's called channels
with Cloud Trail Lake.
This allows you to integrate with events
that are outside of AWS even.
So if you want to also query stuff that's happening
outside of AWS as part of this, you can do that.
It has built in support for Okta, LaunchDarkly, Clumio
and other CloudTrail partners.
I'm sure that list will change over time,
so, wouldn't expect to be quizzed on that exactly.
You can also set up your own custom integration.
So if you want to create a channel
to your own data in your own proprietary source,
that is also possible.
Going back to advanced event selectors,
let's look at an example of one.
So if I want to, for example, log all management events,
but I also wanted to log,
not all resource events,
but just put and delete object calls
within a specific bucket prefix.
Here's an example of how you would set that up.
So the top part of this is saying I'm going to have
a field of event category equals management.
So that's just like checking off the management check box
in the UI that we saw in the console that says,
I wanna log all management events.
But instead of saying I want to log all resource events,
I'm gonna say no, actually all I want
is this very specific thing,
where I want to log put object and delete object events
for two S3 prefixes.
So under the field selectors here,
we're saying the event category has to be data.
The resource type needs to be AWS S3 object.
The event name needs to be put object or delete object.
And the resources ARN needs to start with either,
mybucket slash prefix,
or mybucket2 slash prefix2.
So as opposed to just enabling
all of the data event category, we're saying no,
we want this very specific subset of my data events.
And again, this is an important tool
for controlling your ingestion
and controlling your storage costs
when using Cloud Trail Lake.
'Cause again, if you're not careful, it can add up fast.
Once you have all that data, what do you do with it?
Well, it has this nice little UI for querying it using SQL.
And it also has something called Lake dashboards
to let you visualize those events as well.
But more powerfully you can roll your own SQL queries,
just like we're seeing here on the right.
So there's a bunch of sample queries
that they give you to start from.
So you don't necessarily have to write these from scratch
for common things you might wanna do.
An example here is on the right,
where we're selecting the event time and the user identity
and the region from some data store that we have here,
we're constraining that to a specific range of event times
and to two specific regions.
And we're looking specifically for console login events.
So this will gimme back a report of everybody
who logged into the AWS console
from this account during these times.
And remember, a best practice
for optimizing these query performances
is to bound your queries by event time
to constrain both the execution time and the costs.
So again, there can be a lot of data in here,
and if you end up scanning all of your data by mistake
because you did not constrain that by event time,
you might end up running a very expensive query,
not just in terms of time, but also in terms of money.
So that might be something they might expect you to know.
How do I control my costs when querying CloudTrail Lake?
Always remember to constrain that by event time.
