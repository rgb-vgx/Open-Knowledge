# 163. Amazon Kinesis Data Streams - Consumers
# Section: Analytics
# https://ua.udemy.com/course/aws-data-engineer/learn/lecture/40305594
# Caption: en_US (manual)

Now let's talk about how we can
consume data from Kinesis Data Streams.
And we'll talk first about the classic consumers.
So the first one is the Kinesis SDK.
So the same way we could use a CLI
or some programming language.
To push data through Kinesis Data Streams,
we can use the SDK or the CLI to read data
from Kinesis Data Streams.
We can also use the Kinesis Client Library,
and I hinted at it in the previous lecture.
It's named KCL.
So we produce with the KPL and read with the KCL.
There's also the Kinesis Connector Library,
which could be abbreviated to KCL but actually isn't.
So it's a bit different from Consumer Client Library.
And then we have third party libraries,
such as Apache Spark, Log4j, Flume,
Kafka Connect, all these things.
But the exam expects you to know that Apache Spark
is able to read from Kinesis Data Streams as a consumer.
We can also use Kinesis Data Firehose,
and also AWS Lambda if we need to.
There's this mechanism of consumption
called Kinesis Consumer Enhanced Fan-Out,
and I will discuss it in the next lecture.
So for now, let's just consider how a classic consumer
would work on Kinesis.
So first, the SDK.
Get records.
So this is Classic Kinesis
and that means that the records are going to be polled,
or pooled by the consumer from a shard.
And each shard will get a maximum of two megabyte
total of aggregate throughput.
So each shard, remember, one megabyte of producer,
two megabytes of consumer.
So here's the example.
Our producer is producing two Kinesis Data Stream,
and it's formed maybe of, let's say three shards.
And so if we have three shards,
then we have six megabytes of aggregate
for boots for downstream.
But each shard itself will get two megabytes for its own.
So now we have a consumer application
and it wants to read from, for example, shard number one.
What it will do is that they will do a Get Records API call
and the shard will return some data.
And if the consumer wants more data,
it needs to do another get records API calls.
So that's why it's called a polling mechanism.
So Get Records every time you run it,
it will return up to 10 megabytes of data.
And then because the 10 megabyte
of data goes over the two megabyte per second total,
you will need to wait five seconds
until you do another Get records,
or it will return a maximum of up to 10,000 records.
Now that means, what is that?
There's also another limit you need to know.
First, is that there's a maximum
of five Get Records API calls per shards, per seconds.
So that means that your consumer application,
it can not do just Get Records, Get records
like 20 times per second.
It can do it only five times per second.
That means that you will get 200 millisecond latency
on your data.
So remember that number because it's really important.
But now what does that mean?
If we look at these constraints
and we start adding more consumers.
Well if five consumers application
consumed from the same shard,
they're different applications
and they all needs to read the same data,
then each consumer basically can poll for once a second,
and can receive less than 400 kilobytes per second.
So that means that the more consumers you have,
the less throughput you will have per consumer.
So if we add consumer B and consumer C,
they will all share that limit
of two megabyte per second, per shard.
And they will all share that limit
of five Get Records API call per second.
So it's really important to understand that.
And we'll see how Kinesis Enhanced fan-out out for consumers
will solve that problem.
So the next thing we're going to explore is
the Kinesis Client library.
And it is a Java first library
but it exists as well for other languages too
such as Golang Python, Ruby, Node, and .NET.
And the idea is that using the KCL you can read records
from Kinesis produced with the KPL
because it has a de aggregation mechanism
within the library.
It also has the possibility to have multiple consumers
share multiple shards using the concept of a group.
And that means that there is a shard discovery process
included in the KCL.
Also, there's a check pointing feature
in case your applications go down
or your record sources go down,
then they can resume from wherever
they were last processing.
It also leverages Dynamo DB
for the coordination and check pointing.
So that means that there's going to be
new dynamo DB one row per shard.
And so that means that because dynamo DB is involved
in the process to read from a Kinesis data stream,
that means that you need to take care
of your provisioning of your Dynamo DB table.
That means that you need to have enough
write capacity unit, so WCU
and read capacity unit, so RCU.
Or if you just don't want to worry about it,
you can use the on demand mode for Dynamo DB
and not get any throttling exception.
Because in case you have exceptions on your Dynamo DB table,
then Dynamo DB will throttle
and that may slow down your Kinesis Client Library
to read from Kinesis itself.
Now record processors will process the data.
And then if you get, as I said,
an expired iterator exception,
that means that something is going on in your KCL library.
That means that you need to increase the WCU
because your Dynamo DB table is not fast enough
to keep up with the writes.
So this is an exam question.
So let's have a look at how it works.
So we have a Kinesis data stream
and it has maybe four shards
and we have a Dynamo DB table that will be used
for the check pointing and coordination
of our KCL record processors.
So in this example, I have two KCL applications
of the same group running
on two different EC2 instances.
And so thanks to this shard discovery mechanism
where they can share the charts.
So the first one will be reading from shard one and two
and the second one will be reading
from shard three and four.
And then the KCL applications will be check pointing
the progress into the Dynamo DB table.
And this happens quite often,
that means you need to provide
enough WCU for Dynamo DB table.
And again in case you get an expired iterator exception,
you need to increase the WCU.
Your Write Capacity Units on your Dynamo DB table.
There's also the utterly confusing connector library
also known as KCL, Kinesis Connector Library.
And it's an older Java library from 2016
and it leverages the KCL library under the hood.
And it's used to write data to Amazon S3
or Dynamo DB, Redshift, or OpenSearch.
And the connector library
must be running on an EC2 instance, for example,
for it to happen.
So it's an application whose sole purpose
is to take data from Kinesis data streams
and send it to all these destinations.
Now you may be like,
"Oh, this is already something we can do
with Kinesis Fire hose."
And that's true.
We can already do this with Kinesis Fire hose
and we'll see this.
So for some of these targets,
we can use Kinesis Fire hose,
for example for S3 and Redshift,
but for others we can use AWS Lambda.
So overall, I would say that Kinesis Connector Library
can appear in the exam,
but it's kind of deprecated and replaced
by Kinesis Fire hose and Lambda altogether.
So let's talk about Lambda now.
Lambda can read records from a Kinesis data stream.
And the Lambda consumer also has a small library,
which is really nice,
used to de aggregate record from the KPL.
So you can produce with a KPL
and read from a Lambda consumer using a small library.
Now Lambda can be used to do lightweight ETL.
So we can send data to Amazon S3, Dynamo DB,
Redshift, OepnSearch, or really anywhere you want
as long as you can program it.
And Lambda can also be used to read in real time
from Kinesis data streams and trigger notifications,
or for example, send email in real times
or whatever you may want.
Finally, Frank will describe this at length
but there is a configurable batch size
and we'll see this in the Lambda section,
but basically you can say how much data
at a time Lambda should read from Kinesis
which helps you regulate through puts.
So overall, we've seen all the ways we can read
from Kinesis data streams.
There are many different ones.
But hope it places some context
to which one is good for which use case.
And I will see you in the next lecture.
