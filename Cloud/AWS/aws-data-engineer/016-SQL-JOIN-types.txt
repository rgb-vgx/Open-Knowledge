# 16. SQL JOIN types
# Section: Data Engineering Fundamentals
# https://ua.udemy.com/course/aws-data-engineer/learn/lecture/40394044
# Caption: en_US (auto)

Presenter: Anytime you get tested
on SQL, you can probably expect
to be tested on how different kinds
of joins work, this is kind of a favorite exam topic, not
just for this exam but
for any exam where SQL is involved.
So let's talk about the different kinds of joins.
Now, by default, if you just say join in SQL, what
you're gonna get is an inner join.
So imagine that I have two tables named A and B, and
they both have some column in common, right,
so maybe these are both informations about
some customer, right, and some information
about that customer is in table A, some other
information is in table B but they're tied
together by a common customer ID.
So if I want to reference information
from both of these tables, I could do an inner join based
on that join on the table that I want to combine them
together on.
So with an inner join I'm only going to get results for
the intersection of those two tables,
for every intersection where there is a common customer ID or whatever the
identifier is between these two tables.
Let's look at a example to make it a little bit more clear.
So in this example here we're
tying two tables called customers and payments on the
customer number field, so there
is both a customer's customer number field and a payment
customer number field as well.
What I want to do in this case is select the customer name
from the customer's table and the
payment date and payment amount from
the payments table, that are
tied together by that customer number.
So look at the syntax of this,
we're saying select c. customerName, p. paymentDate,
p. amount from customer
C, so we're giving an alias of C to the customer's table so I
can refer to it in the select
statement as just C instead of customers,
that I'm gonna say inner join payments P,
again, we're giving an alias of P to the payments table
so up top I can refer to payments as just P.
On c. customerNumber, meaning that the customer
number field on the customer's table equals p. customerNumber, and because of that
alias that means the
customer number in the payments table.
So the inner join is only going to gimme results on
records that have a match there, where
there is a customer number in common between
the customer's table and the payments table.
And where those do have a customer number in common, I will select the customer
name, the payment date, and
the amount.
And this is an example of those results here.
So again, inner join, the default join type, if
you just say join, is going
to only give you results for that intersection
where that condition that you're joining on exists
in both tables.
(keyboard key clacks)
We could also do what's called a left
outer join, and in this case we're
going to get results for everything in
table A, whether or not it has a matching identifier in table B.
So in this example here, same thing, except
we're saying left join.
That's the only difference in this query.
But you can see now that we have some null results,
right, so for American Souvenirs Incorporated and
Porto Imports Corporation, whatever it is, we have
nulls for the payment date and amount because,
presumably, there was no matching customer number in the
payments table for that customer
number in the customer's table.
So for whatever the left table is, we're going
to get everything from that table whether
or not it has a match, and if it
doesn't have a match in the right table we'll just
get a null result there in what we're trying
to select from that right table.
We can also do a right outer join,
same exact idea except what we're gonna
do is get everything from the right table,
whether or not it matches, but not
everything from the left table.
So same syntax here, we're saying right join this time, and all that means is,
you know, whatever table
we're having on the right side we'll get everything,
whether or not it
has a match, on the left, but not necessarily everything
from the left side that doesn't have a
match for the right.
You can also do a full outer join, that
will give you the results
for everything in table A and table B, whether
or not there's a match, if there
is a match then you'll get results for that join
statement, if not you'll get
null results, right?
This is often used for debugging and troubleshooting,
so if you want to get a good picture
of where you have mismatched data, where
you don't have matching keys across these
two tables, this can be a good
way to search for it and
see where you have IDs that don't match
up in both tables for some reason.
Finally, we can have a cross outer join, I can't
think of many reasons why you'd want to do this.
What a cross outer join
does is give you a result for every possible combination between the
two tables A and B, so for every
single row in column A I'm gonna give you a result
for every combination
of A and B together.
And as you can imagine, this
blows up really, really, really fast
so not a whole lot of reasons to do this but, just
for completeness, that's that a cross
outer join does, it gives you
every possible combination between
A and B, whether or not it matches.
And just for completion here,
here's what that syntax would look like, cross
join is a syntax for that and, again, it
will give you many, many,
many, many, many, many results because it's
giving you every possible combination between the
customers and the pavements.
