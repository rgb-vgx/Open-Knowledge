# 273. OpenSearch Vector Engines
# Section: Machine Learning
# https://ua.udemy.com/course/aws-data-engineer/learn/lecture/54308705
# Caption: en_US (auto)

Alright, we've talked about OpenSearch at a general level
and how it's generally useful for search,
but how is it specifically useful for a generative AI?
Well, as we've seen before,
Bedrock Vector Stores have a prominent option
for using OpenSearch as your vector store
and it has a vector store capability now too.
And it, not only that, it can be serverless as well.
Now note that serverless is not the same thing
as scale to zero.
As you might learn the hard way,
if you leave a serverless vector store up and running,
you're still gonna get billed a few hundred dollars a month
even if you're not using it.
So be aware of that.
That's not gonna be in the exam, I don't think,
but just telling you that for your own safety.
However, the exam in its current form,
at least treats OpenSearch
as kind of the primary implementation
for backing a Bedrock knowledge base.
Personally, I think that might get subsumed
by the new S3 vectors feature.
There's actually a much easier
and simpler way of doing this now using S3
where you can just throw data in S3
and say, Hey, S3 make vectors out of this.
And then I can tie that to my knowledge base.
I like that a little bit better,
but I think it's just a little bit too new for this exam.
But just to put the idea in your head,
S3 vectors are a thing now too.
Now, vector stores in OpenSearch are not limited
to Bedrock Knowledge Bases.
If you set up a vector store in OpenSearch,
you could use that with other systems as well.
Of course, if you have a system in SageMaker AI,
something built on top of a HuggingFace model
or your own custom models
for retrieval augmented generation,
you can also integrate them into those as well.
So it's not just for Bedrock.
Now something else that might be in the exam
is semantic versus hybrid search.
So semantic is kind of what we do by default, right?
So we say, here's a chunk of text
that I've converted to an embedding vector.
Go find me the closest embedding vector that you have
and what chunk of data is associated with that.
So that is semantic search.
We're just strictly doing vector comparisons there
on embedding vectors.
However, as we've seen
that has some challenges in getting the right relevant data.
So if we combine that
with kind of old school keyword search,
maybe we can get even better results.
So one idea would be to use a hybrid approach
where we combine vector and keyword search together.
And that requires that you're gonna need
to index your keywords for metadata
in addition to these vectors as well.
But it's a powerful technique
for actually improving the relevancy
of your results from the vector store.
So we don't just search for embeddings
and we also search for the actual keyword matches
as well at the same time.
Now getting into the nitty gritty of OpenSearch,
there are three high level engines that we can use
for vector search in OpenSearch.
They are Facebook AI Similarity, FAISS for short,
that's what most people use,
non-metric space library, NMSLib, and Apache Lucene
and a couple of ways of using it.
How do I actually do that vector search?
Fundamentally, I need to find the closest embedding vector
to an embedding vector that I'm giving it,
one way is exact nearest neighbor search.
But as you can imagine, that's pretty slow.
That requires me to compute the distance
from my vector to every other vector
and find the smallest distance.
You don't wanna do that in the real world.
There's a much faster approach
called approximate nearest neighbor,
which is generally good enough.
It might not be perfect,
but for our purposes it's worth the trade off.
And there are two choices here on how to configure that.
One algorithm for ANN is hierarchical navigable small world
or HNSW for short.
And then there's inverted file or IVF.
So the difference here that you wanna remember
is that HNSW is fast, but it uses a lot of RAM.
So if you have a really huge data set,
it might not scale so well.
If you do have a ton of data for a really huge data set,
IVF might be a better choice,
but you're gonna be trading off recall performance
for that extra speed and memory performance.
So if you have a huge data set
and RAM is an issue, IVF is your choice.
Otherwise, if you value speed
and efficiency over everything else,
HNSW is the one that you want.
You can tune HNSW further as well.
There are many parameters,
but these are the three that you should know.
One is M,
which is just how many edges per node are used
by the algorithm.
And you need to just know that a higher M
results in more memory usage.
So it results in a denser graph, higher recall,
but at the expense of more memory.
Then there's ef_construction
and that is the size of the dynamic list
used for the KNN graph internally.
KNN is K Nearest Neighbors,
that's a machine learning algorithm.
That's a different course,
but the higher ef_construction value you have,
the slower your indexing will be,
but your graph will be more accurate.
Then there's ef_search,
which is how thoroughly we explore that graph.
So as you can imagine,
a higher value will give you higher recall values,
you know better quality,
but it will slow down your search performance, okay?
So just remember what the trade-offs are here
of these different parameters for HNSW.
And some more finer points
of using OpenSearch as your vector store,
just random things that might show up on the exam.
There are different vector compression techniques.
So different ANN methods have their own techniques
for vector compression. And it's good that you can do this
and you can imagine, you know,
typically you have a thousand or more dimensions per vector
and if you're just encoding like a 300-character
chunk of text with a thousand dimensions,
you're using more space for the embeddings
and for the data itself it gets a little bit weird,
but they do have vector compression techniques
that can help with that
and typically that's done under the hood.
So you know, you don't have to think about that too much,
but there are a few techniques you should know about.
One is binary vectors.
So if I convert those floating point dimension values
to just bit sequences, that can save a lot of space, right?
So compared to a 32-bit floating point number,
if I can press that down to a single bit,
I've saved 32x compression there.
Now these are not gonna be useful embeddings by their own,
but they can be useful for fast distance search.
So that's a useful tool for accelerating these lookups.
And something else is FP16,
which does still give you a useful embedding vector.
It's just quantizing it from 32-bit to 16-bit resolution.
So still a floating point value in those vectors,
but you know, just with a little bit less resolution
and usually that's fine
and this is what HNSW does actually
by default under the hood.
So that is HNSW's default vector compression, FP16,
remember that one, just means 16-bit floating point values
as opposed to 32-bit.
Some more finer points that you might need to know about.
How do I choose a sharding strategy
when I'm dealing with this stuff?
So we talked about sharding earlier,
but there are some specific considerations
with semantic search.
So we need to balance the shard size and our CPU ratio,
obviously, like you do, we want to make sure
that you have enough CPU horsepower
for the number of shards you have.
That's a given.
But a more fine point here
is that if you're just doing semantic search,
those are actually pretty straightforward queries, right?
So there's not a whole lot of coordination going on
across shards there.
So if you're just doing vector search, semantic search,
you can get away with larger shards as a result,
on the order of 30 to 50 gigabytes.
But if you're doing a hybrid search
where you're still doing traditional keyword search,
you want smaller shards
'cause you'll be doing more cross shard coordination
for those keyword queries, right?
So that's the one to remember.
Now, if you're using the serverless version of OpenSearch,
this all happens automatically under the hood.
But if you are provisioning your own shards,
that is something to know.
So again, semantic, larger shards,
hybrid, smaller shards.
There are also techniques to using multiple indexes
that can be useful as well.
So let's say you are indexing different types of documents
or documents from different domains,
maybe they deserve their own indices.
So that way you can tune performance individually
for each type of data, for each corpus of data.
And maybe you want different embedding models as well
for different types of data
that gives you that flexibility,
if you have a multi-index approach for your embeddings.
Now you're gonna need
some sort of a query routing system to pull that off.
So I need to know which index do I hit
for a given type of data
and one way of doing that is hierarchical indices.
So the idea here is that you have a top level index
whose only purpose is to route you to a detailed level index
for a specific kind of document.
That top level one is really small and fast,
it just routes you to an index
with more detailed data per domain.
So again, it's a way of implementing these multiple indices
we just talked about that might be specialized
for different data types or for different domains.
And again, they could have their own shard sizes,
their their own tuning,
their own embedding models, whatever it might be.
Now there are other ways of doing the same thing.
You know, you could imagine a foundation model
making that decision for you as well,
but this is just a little bit more
of a low level way of doing that.
One more thing, the neural plugin for OpenSearch
as a vector store, you might need to know about that.
This is just a way to integrate your embedding model
directly into OpenSearches ingest pipeline.
So the neural plugin for OpenSearch lets you include
that embedding model as part of the whole pipeline.
So it allows you to connect your embedding model
with OpenSearch.
So you can just throw text at OpenSearch,
it will figure out how to compute
the appropriate embedding vector
and then do its vector query automatically
is one big thing.
So if you do want to package everything
that you need into OpenSearch itself,
that can make it an alternative
to using a Bedrock Knowledge Base
because OpenSearch at this point can just take raw text,
you know, do that vector search
and give you back the most relevant chuck all within itself
by using the neural plugin.
Another thing that neural plugin does,
it does integrate with Bedrock.
So it allows you to actually call your Bedrock
in embedding model as part of that search process.
So you know, it doesn't have to be a raw model
that you're passing in,
it can actually hit that from Bedrock
to embed that within OpenSearch itself.
So if you want that embedding to happen within OpenSearch,
that's what the neural plugin is for.
That's what you need to remember.
