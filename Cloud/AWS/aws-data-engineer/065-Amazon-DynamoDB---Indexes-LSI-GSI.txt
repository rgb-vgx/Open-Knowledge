# 65. Amazon DynamoDB - Indexes (LSI & GSI)
# Section: Database
# https://ua.udemy.com/course/aws-data-engineer/learn/lecture/40304560
# Caption: en_US (auto)

Narrator: Okay, so now let's talk about indexes
for DynamoDB and you have two kinds of indexes
that you need to know.
The first one is a local secondary index.
So LSI is going
to give you an alternative sort key for your table.
So you have the same partition key from your base table,
but you're going to get an additional sort key.
And this sort key consists of one scalar attributes.
So it could be a string, a number or a binary.
And you can get up to five LSIs per table.
Now LSIs must be defined at table creation time.
So you cannot create them after your table's being created.
So you need to have some careful thinking into
how you want your table to be designed.
Next on your LSI,
you can have some or all attributes you know
from your main table.
So it's up to you to choose in your LSI if you want
to only have maybe one specific attribute
because this is what you're trying to query for.
So if we take an example of here,
this is our table which has user id, game id,
game timestamp, score, and results.
Okay and right now we can do queries on user ID
and game ID very simply,
but we cannot do a query on user ID
and game timestamp for this,
we'll need to do a scan and then do some service side,
some client side filtering.
So to do so, if you wanted to do a query based on user ID
and game timestamp,
we would need to create an LSI
and define the LSI on the attribute game timestamp.
And if we do so, then we can do a query on, hey,
give me all the games that have been done by this user
between 2021 and 2020 and so on.
Okay, so this is super important for you to understand.
This is the same partition key as before,
but we have a different sort key thanks to an LSI.
Next we have GSI or global secondary indexes.
So this is going to give you an different,
so an alternative primary key.
So you could have a different hash key,
a different partition key,
or you can have a different hash key and sort key as well
from the base table.
So this is helpful if you wanted
to speed up queries on things
that are non-key attributes within your table.
So the index can consist of scalar attributes,
so string number and binary.
And again, you can specify which attributes you want
to project on that index.
And for that index though,
it's very special
because it's kind of like a different new table.
And therefore for this index,
you must provision the RCU and WCU's.
Now the GSIs are powerful because they can be added
or modified after the table has been created.
So let's take a very simple table in which we have user id,
game ID and game timestamp.
With this table we're able to query based on user id.
So give me all the games of this user,
but we cannot query on game id as we can see right now,
if you wanted to query on game id,
it would be extremely difficult you need to do a scan
and then filter client side.
So instead we're going to create a GSI,
a global secondary index, okay?
Which is going to give us the ability to query by game id.
So now the partition key for that GSI becomes the game id.
The sort key may be, for example,
game timestamp if this is what you wanna query on
and the attributes become user ID
because we've done a projection of the user ID attributes.
So you can see in this one,
we're creating some fully different new queries
by defining a new partition key and a sort key,
and this is why it's very important DynamoDB
to understand how you're going to query data to understand
how you're gonna make your local secondary indexes
and your global secondary indexes.
So we've seen LSI, we've seen GSI very,
very different purposes,
but let's talk about these indexes and throttling.
So when you have A GSI,
if the rights are throttle on the GSI,
then the main table will be throttled as well.
So this says a very, very important caveat
that comes up in the exam.
So even if the WCU are fine on the main table,
if they're throttling on the GSI,
then the main table no matter what will be throttling
and therefore choose your GSI partition key carefully
and assign your WCU capacity very, very carefully.
Whereas for LSI, so local secondary indexes,
they will use the WCU and the RCU of the main table
and there's no special threat link considerations to be had.
Okay?
So that's it for this lecture.
I hope you liked it and I will see you in the next lecture.
