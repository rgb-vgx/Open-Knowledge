# 15. SQL Review: Aggregations, Grouping, Sorting, Pivoting
# Section: Data Engineering Fundamentals
# https://ua.udemy.com/course/aws-data-engineer/learn/lecture/40394036
# Caption: en_US (auto)

Instructor: The exam guide says you need to know
structured query language or SQL
and some specific things that you wanna do with SQL.
So let's do our review here.
Now I'm gonna assume that you know what SQL is.
If you're a complete beginner to SQL,
you've never done it before,
and you don't even have any idea
what this thing on the screen means,
I would suggest adding a SQL course
to your learning path for this exam.
I can't include an entire course on SQL
as part of this course.
This is just gonna be a quick review
of some of the main points
that the exam expects you to understand.
If you need more depth in that,
then there are lots of great SQL courses out there.
Just go find the most popular one,
you'll be in good hands, I'm sure.
They do say you need to understand aggregation,
which is pretty basic stuff, so let's start there.
If you've ever done any SQL at all,
you know how to get the number of rows in a table.
You just say SELECT COUNT(*).
And in this example I'm saying, AS total_rows.
AS is just assigning the name of the column
that I'll see in the results from that query.
And FROM employees means that I'm counting them up
from the table named employees,
so that will give me the total number of rows
from the employee's table.
COUNT(*) does that.
If I want to add up
all the salaries in the employee's table,
I could say SUM(salary).
And display that result
under a column named total_salary in this example.
If I wanna get the average salary,
AVG is the operator for that.
I could just say AVG(salary) AS average_salary.
Again, the AS is just naming the column
that has the result in it from the employee's table.
And I can get the maximum and the minimum as well.
So if I wanted to get the maximum salary,
the highest paid salary in our entire table,
I could say SELECT MAX(salary),
and call that result highest_salary FROM employees,
so pretty basic stuff.
If this does not make sense to you, again,
you might want to go take a separate SQL course
'cause this is just a quick review.
One thing that's a little bit more than basic
is using the CASE statement, let's talk about that trick,
so the context here is
try to apply filters to your results
when you're aggregating.
Now if you only need a filter on one condition,
you don't need a CASE statement,
you can just use a WHERE clause
that's a very common thing to do, right?
So if I wanted to get the total number of count
of employees,
but I only wanted the number of employees
that make more than $70,000, pounds,
whatever your unit of currency is,
I could just tack on a WHERE salary > 70,000
at the end of this statement, right?
So what that's gonna do is say
SELECT COUNT(*) AS high_salary_count.
So I'm gonna have a column named high salary count
from the employee's table.
But instead of counting up every row in that table,
I'm just gonna count up the rows
where the salary column is greater than 70,000.
Makes sense?
So what if I want to do more than one,
more than one WHERE clause, right?
What if I have more than one filter that I want to do?
Well, one way to do that is with a CASE statement.
So the CASE clause allows you to specify some condition
and I can actually do more than one at a time here, so.
The thing with the WHERE clause
is that since you specify it
after the aggregation statement,
I can only filter on one thing at a time
using a WHERE clause.
But if I move that filter within the actual SELECT itself,
then I can do more than one thing at a time.
So in this example here, I'm saying
SELECT COUNT (CASE WHEN salary > 70,000 THEN 1 END),
what does that do?
That means that I'm gonna
apply this little bit of logic here
where for every row, if the salary is more than 70,000,
I'm gonna return the value one.
All right?
So that's gonna give me a count
of all the values that have one
because they have more than 70,000
and that account will be displayed as high salary account.
At the same time, I have another clause here
(CASE WHEN salary BETWEEN 50,000 and 70,000 THEN 1),
so the then one just means that I'm gonna add up that one
to get the total number of counts for that condition
and I would call that medium salary count.
And similarly, for anyone less than 50,000, same idea,
just (CASE WHEN salary &lt; 50,000 THEN 1 END)
and that will count up all the ones
that result from that condition
and display that as low salary count.
So one little trick for doing multiple filters at once
using the CASE statement,
you might not have seen that one before.
Grouping quick review here.
So what if I want to split things up
and kind of break things down by a given field?
So in this example, I want to count up
how many employees are in each department?
And let's imagine that our employee's table
has a department_id column for every person
and we could do something like this.
So SELECT department_id, COUNT(*) AS number_of_employees.
So that will display the result of the department_id column
and then the total number of the results
that I'm gonna query for,
and label that result as number of employees.
From the employee's table.
We're applying a condition here just for the fun of it
where join_date > January 1st, 2020.
Oh man, if only they knew what they were in for
a few months from then.
And we're gonna say GROUP BY department_id.
That's the point here, so that's grouping.
By saying GROUP BY department_id,
I'm gonna get a different row in my results
for each unique department_id
that exists in that employee's table.
So here's a sample output from that.
It would have the department_id
and the number of employees,
those are the labels
that I selected in the SELECT statement.
Where these are all the people
that joined after January 1st, 2020,
but then grouped by their department_id.
So I'm seeing here
that there's a total of five employees in the HR department
that joined after January 1st, 2020.
And a total of eight employees in the IT department
that joined after January 1st, 2020.
So just a reminder of how GROUP BY works.
We can also GOURP BY more than one thing
and we call that nested grouping.
So if I were to say GROUP BY sale_year, product_id,
what's going on here?
This is a different example here.
Let's say we have a sales table here where we have,
when things were sold, what product was sold,
and for how much, right?
So here we're saying select the year of the sale date
and display that as sale_year.
And then the next thing we're gonna have,
the next column in our results is just the product_id.
And then the sum of all the amounts
for that product_id in that year is what that means.
And we're gonna call that final sum total sales.
FROM sales,
that's just the name of the table that we're working with.
And then we're gonna say GROUP BY sale_year, product_id.
Now sale_year of above top
is just referring to the year of the sale date, right?
So we're gonna group by the year that sale was made
and also, by the individual product_id.
So what this is gonna do
is give me individual rows in the result
for each combination of the sale year and the product_id.
So for every product
I'm gonna see the results for every year.
Now, what if I wanna order that by a specific criteria?
That's what ORDER BY is doing, that's sorting my results.
In this case we're ordering by sale_year
and then by total_sales in descending order.
So what this is giving me is a table
that for every year is gonna give me the list of product_ids
that were sold in that year sorted
by the total number of sale, the total sale amount
for that product in that year.
So a little bit more of a complicated example there
of nested grouping and sorting at the same time.
So try to wrap your head around that one.
Pivoting also worth talking about.
Usually when we talk about pivots,
it's in the context of a Microsoft Excel spreadsheet
or something like that,
but you can also pivot in a spreadsheet as well,
it's possible.
The syntax will vary depending on what database you're in.
So I wouldn't really expect the exam to quiz you
on the specific syntax of how pivoting works.
So don't get too bogged down in the syntax here
and how it all works at the SQL level,
I kind of doubt they'll actually quiz you on that.
The important point is just knowing what pivoting is.
So the main takeaway here
is that pivoting is the act of turning row-level data
into columnar data, right?
So I'm taking a table and just kind of flipping it on set.
And how that works, again, is specific to the database.
Some have a PIVOT command.
So here's an example of a database
that does have a PIVOT command.
We wanna start from the inside of this query, right?
So let's look at that second SELECT statement
there in this query, where it says
SELECT salesperson, month, sales FROM sales AS sourceTable,
all right?
So let's imagine that we have a sales table
that contains sales amounts and the salesperson
in each row, but we wanna report by salesperson.
So to do that we gotta flip things around, okay?
So that's what this PIVOT statement in the middle is doing.
We're saying pivot the total number of sales for each month
in January and February.
So we're just going to sum up the sales for January
and sum up the sales for February and flip that over,
and we're gonna call that pivot table.
From that pivoted table,
we'll select the salesperson, the month,
and the total number of sales from that pivoted table.
And we're gonna call that sourceTable.
Outside of that, we have another SELECT statement
that's really just renaming those results.
So we're saying that for Jan,
we're gonna call that Jan_sales,
and for February we're gonna call that Feb_sales.
And the output here is the pivoted result
where instead of a row that has salesperson in amount,
we now have a row for each individual salesperson
and the sum of their sales
in each month of January and February.
So, PIVOT is one way of doing it.
Another more portable way
is just by using conditional aggregation.
So again,
we can go back to our friend the CASE clause there.
We could have done the same thing
by just saying SELECT salesperson
SUM (CASE WHEN month = January THEN sales ELSE 0).
In that case, we're going to sum up the results
of all the sales that were in the January month.
And if it was not a January month,
then we will just add up zero
so that we don't actually count any other months
in that case.
Same thing for February there
and we get the same exact results that way.
So maybe a little bit of a hacky way
of using aggregation to do a pivot.
Maybe not the most efficient way of going about it,
but if you don't have a specific pivot operation,
it's another way to do it.
Again, the main takeaway here
is just that pivoting is a way to take row level information
and make it columnar.
It's just flipping a table around.
