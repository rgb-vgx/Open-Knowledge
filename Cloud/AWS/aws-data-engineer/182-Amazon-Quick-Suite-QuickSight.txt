# 182. Amazon Quick Suite / QuickSight
# Section: Analytics
# https://ua.udemy.com/course/aws-data-engineer/learn/lecture/40356640
# Caption: en_US (auto)

Instructor: Let's dive into Amazon QuickSight,
which is AWS's visualization tool for big data.
What is QuickSight?
Well, the official definition from Amazon
is it's a fast, easy,
cloud-powered business analytics service.
Let's break that down.
So, yeah, it's basically a service
for visualizing your data,
and the fast and easy cloud-powered bit
means that it's scalable
and it's not just meant for developers.
It's really more of a web application
where anybody in your organization can use this
to look at dashboards or create their own dashboards,
create their own charts and graphs,
whatever they need to do to interpret your data
and look for trends and stuff from a business standpoint.
So it's made for people that are data savvy,
but not necessarily a developer.
It allows all the employees in an organization,
not just the developers,
to build visualizations on top of your data.
You can also build paginated reports for management.
This is actually a new feature in QuickSight,
believe it or not.
You think it'll be there,
but they just recently figured that out.
You can use it for performing ad-hoc analysis.
So it can be sitting on top of your data lake,
your data warehouse, whatever your data is,
we'll talk about those sources momentarily,
and get real-time visualization of that data as it changes.
So if you wanna dig into the latest data
and understand how it's behaving, what the trends are,
what that means for the business,
you can very quickly set up new visualizations,
take a look at the data, interpret it,
try to make sense of it, try to apply that to your business.
You can also get alerts on detected anomaly.
So there's a feature
where it can automatically monitor your data and say,
"Hey, something's out of the ordinary here.
Maybe you should do something about it, person."
You can also, more generally,
just use it to get business insights
from your data very quickly.
That's the whole point of QuickSight.
That's the quick part I guess.
So it's supposed to be a relatively easy way
to visualize your data.
You don't necessarily have to know SQL, although it helps,
but there are some features that will automatically
try to create a appropriate visualization
just by inspecting the data and trying
to figure it out on its own as well.
So knowing SQL helps.
You don't have to though.
And it's built for any device.
So even if you're a CEO who wants
to look at the latest dashboards
for the business on their cell phone,
QuickSight can be the front end for that.
And it's fully serverless of course.
You know, we don't want your CEO
having to worry about
how many servers are under the hood, powering QuickSight.
It all scales up and down for you automatically.
There are many data sources QuickSight can pull from.
So where is the data that you want it to visualize?
That is something you might have to worry about
as a developer.
The obvious choice is Redshift.
You might have a giant data warehouse of course,
and you just want to make some charts, and graphs,
and reports on top of that data warehouse,
QuickSight can do that of course.
You can sit on top of any Aurora database
or RDS database.
So if you're running some, you know,
MySQL instance out there
or some SQL database of some sort,
you can put QuickSight on top of that as well.
Athena, also a popular choice.
So if you have a big data lake
sitting in S3 or something,
you can have Athena on top of that
and then visualize through Athena
that data in your data lake.
So that's kind of a cool capability, right?
Without even having a "real database".
You can just have a collection
of unstructured data in S3 somewhere
and not only query that through Athena,
but also visualize that data using QuickSight.
It can also talk to OpenSearch.
So a little bit more of a obscure use case there,
but you could put QuickSight
on top of an OpenSearch database if you wanted to.
Often people will put their, you know, server data,
their server log data into OpenSearch.
So if you want to visualize that
and look at things like server latencies
or, you know, response codes over time,
you can also do that with QuickSight as well.
It also integrates with IoT analytics, internet of things,
although AWS is not emphasizing that as much these days.
And if you have an EC2-hosted database,
maybe you just put your own MySQL instance
on an EC2 instance somewhere,
QuickSight can talk to that too.
Also, just draw files.
You can just have CSV files or TSV files,
tab separated value or comma separated value files,
or even Excel files,
or even common log formats just sitting in S3 somewhere
or sitting somewhere on-premises, you know,
someplace that QuickSight can get to somehow,
and it can visualize that directly as well.
Before you analyze or visualize that
you can even prepare that data if you want to,
to a limited extent within QuickSight.
So if you need to do things
like change the names of columns in your file,
you can do that.
Change the field names, add calculated fields,
you can use SQL queries there.
You can change data types
if you need to do a little bit of massaging of the data
before you visualize it,
you can do that within QuickSight too.
An important piece of QuickSight is SPICE,
and this is its trick that it uses
for accelerating its queries.
SPICE stands for super-fast, parallel,
in memory Calculation Engine.
So it's a way of copying your data from Athena
into a more optimized format
that leads to faster queries from QuickSight.
So the idea is that it uses columnar storage,
in-memory access, machine code generation,
and the idea is to accelerate your interactive queries
on very large data sets.
Now this does take space of course, storage capacity.
It basically needs to copy a lot of your data
from Athena into this more compact
and optimized representation that SPICE uses.
Every user gets 10 gigabytes of SPICE by default.
It is promised to be highly available
and highly durable just like S3 would be,
and it can scale up to hundreds of thousands of users.
So they've made it available, durable, and scalable.
So the idea of SPICE again
is just to accelerate your queries,
but it is an an extra layer there.
So you have to think about that sometimes.
So, although it can accelerate very large queries
that would normally time out
if you're using direct query mode,
direct query mode is another word
for hitting Athena directly.
You can get into a situation
where a queries are too big even for SPICE.
So one situation you might encounter
is that if it takes more than 30 minutes
to import your data from Athena into SPICE,
you're still gonna time out.
So, even though SPICE is designed
to accelerate large queries with QuickSight,
there's still an upper limit there that you might hit.
So something to be aware of there.
But just remember,
SPICE is the super fast, parallel,
in-memory calculation engine
that exists to give you a more
optimized representation of your data
and accelerate your interactive queries through QuickSight,
especially on large datasets.
But, you know, it is possible to get so large
that even SPICE can't handle it.
Here are some of the use cases
that Amazon recommends for QuickSight.
One is the interactive ad-hoc exploration
and visualization of data.
So you might recall
that this is actually explicitly an anti-pattern
in other services.
However, QuickSight is designed from the ground up
for ad-hoc exploration and visualization of your data.
You can also create dashboards with QuickSight.
So you can make these nice to look at dashboards
to visualize data on a periodic basis
and make sure that your executives can see those
and see how the business is doing.
It's also useful for analyzing
or visualizing data from a variety of sources,
including S3 logs, any databases you might have on premise,
other AWS services that we listed earlier:
RDS, Redshift, Athena, and S3.
You can also integrate QuickSight
with things like Salesforce, which might be useful to know.
Any software as a service application
can probably integrate with QuickSight
and any JDBC or ODBC data source.
So, pretty much any data source you can imagine,
QuickSight can import it
and allow you to visualize it very quickly
and at large scale.
However, there are some things QuickSight is not made for,
and although it has limited ETL capabilities,
if you want to do more powerful
or larger scale ETL transformations on your data,
QuickSight really isn't the tool for that.
You wanna be using Glue ETL instead or maybe Apache Spark.
But again, QuickSight does have
some limited transformations you can do
just to make your data look a little bit better.
From a security standpoint,
it offers multi-factor authentication
on the accounts that access QuickSight.
Remember this is more of an end user tool,
so you're not gonna be using
lower level security features with this.
It does have VPC connectivity, however.
So you can add QuickSights IP address range
to your database security groups.
You will have to worry about
how QuickSight will communicate with your databases
and get through the security you have on them.
It also offers row level security
or RLS that enables QuickSight dataset owners
to control access to data at row granularity
based on permissions associated
with the user interacting with the data.
And with RLS or row level security,
QuickSight users only need to manage a single set of data
and apply appropriate row level dataset rules to it.
In late 2020, they also added column level security or CLS.
This is only present
in the Enterprise Edition of QuickSight,
but it does allow you to manage security at the column level
as well as the role level now.
You also have private VPC access available
that's made possible
through the Elastic Network Interface or ENI
to create a secure private communication with data sources
and a VPC that also uses AWS Direct Connect
to create a secure
and private link with your On-Premises resources.
So if you do want to use QuickSight
on top of On-Premises databases or files,
you can use Direct Connect to do that.
A little more on security and QuickSight.
So how do you manage resource access?
Well, it's pretty simple, really.
Right from the QuickSight console
there's a managed QuickSight menu,
and from there you can go to security and permissions
and from there you can ensure that QuickSight itself
is authorized to use Athena S3
and specifically your S3 buckets.
So before you get to the user level,
you have to make sure that QuickSight itself
is authorized to access the data that you want,
whether it's in Athena or S3.
And again, that can be managed
right from the QuickSight console.
Now, if you do need to restrict
what users can access, specific users,
you can create IAM policies to restrict what data in S3
a given QuickSight user can access.
So remember, you need to ensure QuickSight itself
is authorized to access your data,
and you can also need to control what users can access
what data through IAM policies.
So QuickSight itself,
you can just manage that through the QuickSight console,
but if you do need more granular permissions
at the user level, IAM can be used for that.
So there's also the specific issue of QuickSight
and Redshift together from a security standpoint.
And generally speaking,
QuickSight just tries to handle all this stuff
under the hood for you
'cause it's really aimed more at end users
and not AWS experts,
but this is one area that can trip you up.
So the thing is, by default,
QuickSight can only access data stored
within the same region
as the one that you're actually running QuickSight within.
So you could have a situation
where you have an existing Redshift cluster
running in some region like, you know, Japan or something,
but you actually spun up QuickSight in Virginia,
and in that case you're gonna get errors
because those two regions can't talk to each other.
You might think you could just create a VPC
and configure that to work across regions,
but that actually does not work with QuickSight.
If you do have this situation,
the way to fix it is to create a new security group
with an inbound rule authorizing access from the IP range
of the QuickSight servers in that region.
So I'm highlighting this for a reason, guys.
Those ranges are fortunately documented,
you have to look them up,
but that is the step you have to take
if you need to have QuickSight access,
a Redshift cluster that's in a different region,
set up a new security group
with an inbound rule with the IP range
of where the QuickSight servers are.
And you gotta look up what those IP ranges are.
Now if you have the Enterprise edition of QuickSight,
there are are ways of handling the cross region problem
of getting from QuickSight in one region
to Redshift or even RDS data
that might be in a different region.
And these same techniques, in some cases,
also work for cross account access.
So again, if you have Enterprise Edition,
then you have the capability
to create a private subnet within a VPC
and then use Elastic Network Interface
to put QuickSight inside of that subnet inside your VPC.
Now, accessing QuickSight from within a private subnet
is the part that requires Enterprise Edition to do,
but once you do that,
all the various techniques for connecting subnets together
across regions or across accounts become available to you.
So let's dive into where that goes from there.
So this is step one, no matter how you're doing it.
You need to put QuickSight within a private subnet in a VPC
using Elastic Network Interface or ENI for short.
And one way to do cross region access
is to also put RDS, or Redshift
or whatever your data is inside another private subnet,
and then connect that with your private subnet
that has Quick Knight using a peering connection, okay?
So basically you put QuickSight
inside a private subnet.
You have RDS or Redshift inside another private subnet
that might be in a different account,
might be in a different region,
but you can still tie them together
using a peering connection to gain access across regions.
This is the sort of thing
that the exam loves to question you about
so try to internalize this.
Now, this isn't only useful for cross region access,
it can also be used, in some cases,
for cross-account access.
And if your goal is cross-account access,
there's a few other ways of doing it as well.
Basically, if you replace the peering connection
with AWS Transit Gateway,
that's another way to connect two different subnets.
However, Transit Gateway itself
is limited to being within the same org and region
between the things you're trying to connect.
However, it is possible to peer Transit Gateways together.
So this might be a practical way
of connecting things if you want to get cross-account access
when those accounts are in the same org in the same region.
Otherwise, you may as well just use peering, right?
Another approach is to use AWS Private Link.
And basically that would substitute Private Link
for AWS Transit Gateway
in that top diagram there, same idea.
Another technique is using VPC Sharing.
And you can see that
in the bottom diagram here on the right.
That's taking two private subnets,
one for QuickSight, one for your data source,
and then using VPC Sharing to tie them together.
Again, a way to provide access
across two different accounts.
You can see the diagram here
talks about account A and account B.
So that's really the goal of these approaches here.
So, remember, these are all ways of handling cross region
and cross-account access.
If your goal is cross region access,
peering is probably the way to go.
But if you want cross-account access,
Transit Gateway, Private Link or VPC Sharing
might be viable depending on your requirements.
Talk a bit about QuickSight User Management.
How do you actually manage who has access to it?
Again, you know, AWS tries to make QuickSight
end user friendly.
So you can define users in IAM and just do it that way.
That gives you more strict control
over what individual users can
and cannot access in terms of what data,
what underlying S3 buckets, what Redshift databases,
what have you.
Or you can actually configure it
so people can just sign up over email
and get approved that way.
So that's a more easy way to set things up
if you're comfortable with it.
Also important to understand
is that there is active directory integration
available in QuickSight as well.
There's something called Active Directory Connector.
This is only offered in the QuickSight Enterprise Edition.
And again, in the spirit
of making things as easy as possible,
with Active Directory Connector,
all the keys are managed by AWS for QuickSight.
You cannot use customer provided keys
with Active Directory with QuickSight.
Also, remember, Active Directory Support
is only in the Enterprise Edition.
Now what you can do is set up different groups of users
that correspond to active directory user groups
and tweak their security access using IAM if you need to.
So you can still set up groups of users
through Active Directory
and tweak what they can and cannot access using IAM.
So the takeaway here is,
the base starting point of security in QuickSight is IAM.
You can use that to define what users can or cannot do.
Email signup as an option.
And if you're using Enterprise Edition,
you can use Active Directory Connector,
but remember, the keys are all managed by AWS.
You cannot use customer provided keys,
and it is only supported in the Enterprise Edition.
