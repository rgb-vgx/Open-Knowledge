# 223. AWS Services Security Deep Dive - Part 3
# Section: Security, Identity, and Compliance
# https://ua.udemy.com/course/aws-data-engineer/learn/lecture/40306674
# Caption: en_US (manual)

Hey, so we are in part three
of our security analysis of the services.
And we are starting with EMR.
Now, EMR is something that's going to be extremely important
to understand the security of going into the exam.
And so as such, you need to pay a lot of attention
into the next two slides,
and you need to remember everything I'm going to say.
So, let's get going.
So first, because EMR provisions some EC2 instances,
it is possible for us to provision a key pair
to manage SSH access into these instances.
We can also attach IAM roles to these EC2 instances.
Why? Well, because maybe we want to access S3 using EMRFS
for doing the request, or maybe you want to write data
back into S3 for whatever reason.
So, you need to make sure that the IAM roles
have access to S3 obviously.
If you are accessing DynamoDB,
and you need to use Hive for this,
then you need to make sure again,
that your EC2 instances are permissioned to access DynamoDB.
And again, this IAM role is crucial to achieve just that.
Now for security groups, you have two kinds.
You have the first kind, which is for the master node,
and the second kind for the cluster nodes,
which represents the core node and the task node.
And the reason why we have security groups
is to allow node to node communication,
because say, you are running a Spark job
or a distributed match reviews job
or anything that does require your nodes
to talk to one another.
Then you need to make sure
that the network security rules are in place
to allow these communications.
So, security groups are extremely important.
In terms of authentication,
we have Kerberos authentication.
And this is helpful when you want to connect
your authentication mechanism to Active Directory.
And then finally, there is an open source way
of managing security on EMR for authorization
which is called Apache Ranger.
So, it's RBAC - Role Based Access,
and you need to set this up on an external EC2 instance
and then connect it to your EMR cluster.
So, that's not something you can set up directly from EMR.
You have to set up externally on EC2
and connect it, set it up with your EMR cluster.
I would recommend reading that blog
if you want to know more about this EMR security.
I think it's a very interesting blog to read
and something that could be very useful
going into your exam.
Okay, the next for EMR is going to be around EMR encryption
and that's super important.
You remember everything I'm going to say.
So, we have at-rest data encryption for EMRFS.
So, EMRFS is when your data lives on Amazon S3.
As such, we need to encrypt the data in Amazon S3.
So, we have three different options.
We have SSE-S3, SSE-KMS, or even Client-Side encryption.
So, three kinds.
As you can see, the fourth kind SSE-C
is not supported by EMRFS.
You can also encrypt the data in your local disks.
So using this EMRFS, you have both the encryption
happening on Amazon S3 and on your local disk.
Let's talk about these local disk in further.
So, you have different kind of encryption
for your local disk.
The first one is open-source HDFS encryption
which is to rely on whatever encryption has been created
for the Hadoop ecosystem
and use that in your Hadoop cluster.
But you can also use some more
AWS native encryption mechanisms.
So if you have an EC2 instance store,
so that means your storage is attached
physically to your EC2 instance,
then you can use NVMe encryption or LUKS encryption.
Okay? Remember these two things.
And next is you have an EBS volume.
So, that means that your storage
is not physically attached to your EC2 instance.
That means it's attached through the network.
Then, you can have EBS encryption,
the native encryption using KMS.
And the good thing about it is that it works
for the root volume of your EC2 instance.
Very important to remember.
But you can also use Lux encryption.
And in that case, it does not work with roots.
So, if you come to an EMR cluster that is backed by EBS
and you want to make sure the EBS volume
are encrypted including the root volume.
Then, you have to go with EBS encryption KMS,
not LUKS encryption.
Okay, that's for at-rest.
Now, for in-transit,
we can have encryption between your nodes
so that there's node to node communication
that's being encrypted using SSL.
And you can also make sure that any traffic done from EMRFS,
so between S3 and your cluster nodes
is also going to be end-to-end encrypted using TLS.
This diagram can be helpful to remember things
and put it into perspective.
And this comes straight out of the documentation of EMR
for the data encryption options.
So as I said, you have to remember
everything I just said for EMR.
These will be some very valuable points
going into the exam, so hopefully that's helpful.
The next technology is going to be the OpenSearch Service.
So, we have network isolation
through the usage of using Amazon VPC.
And if we wanted to manage access to our OpenSearch cluster
we can use an OpenSearch policy to manage security.
For example, to restrict by IP address.
We have data security
by encrypting the data at rest using KMS.
And we have encryption in-transit using the HTTPS end points
of the OpenSearch Service,
which behind the scenes using TLS encryption.
For authentication,
we have IAM or Cognito based authentication.
For example, if we use Cognito,
then we allow the end user to login
into Open Search Dashboards
through an Enterprise Identity Provider
such as Microsoft Active Directory using the SAML protocol.
So note, that's a lot of keywords,
but important to understand and to know going into the exam.
So, now we have Redshift.
And Redshift is also very, very, very important
to know in terms of security.
So, the VPC will provide you
with network isolation for Redshift,
and you can on top of it apply cluster security groups
to ensure you do have control over which machines
can access your Redshift cluster.
You get encryption in flight
because you're using the JDBC driver enabled with SSL.
So, you can definitely encrypt your data
directly going into Redshift,
and you can encrypt at-rest using KMS or an HSM device.
And so, if you use an HSM device
you encrypt at rest in Redshift,
you need to establish a connection
between your HSM device and Redshift.
All of that is well-documented on the AWS documentation.
What you need to remember is that Redshift
is at one technology that can use either KMS,
and this way it's managed by AWS,
or we can use a custom HSM device
or Hardware Security Module.
It also supports SSE-S3 using default managed keys.
And you can attach IAM roles for Redshift
basically allowing you to dump or read data from S3.
So, okay, finally, if you use a copy or an unload command,
you can reference the IAM roles
that you have assigned to Redshift, or alternatively,
if you wanted to inherit your own key credentials,
you can paste your own access key and secret key
directly into your SQL statements,
and Redshift will leverage those
when reading data from S3 or writing data to S3.
Okay, hope that makes sense.
Again, make sure that you really know
Redshift security very well.
Athena, Athena is very easy.
You use IAM policies to control access
to the Athena service itself.
And the data will be stored in S3 anyway,
so you inherit all the security that comes from S3
that includes IAM policies, bucket policies, and ACLs.
You're going to encrypt data according to S3 centers.
So we have SSE-S3, SSE-KMS,
and CSE means client-side encryption for KMS.
You can get in-transit encryption obviously
because using TLS certificates between Athena and S3,
and you can use a JBC driver
that is going to be enabled by SSL.
Finally, if you need fine grained access,
you can use the AWS Glue Catalog Security itself
to restrict who can do what in Athena.
All right, that's it for the query technologies.
Finally, the visualization technology of Quicksight.
Well, you have two kind of editions.
You have the Standard edition
where you can use the IAM users to log into Quicksight
or email-based accounts.
And for the Enterprise edition
you can use Active Directory-based security
to login to Quicksight.
With a Federated Login,
it also supports MFA or multi-factor authentication.
You get encryption at rest and in SPICE as well.
So, you get encryption at rest within Quicksight
and within the processor engine.
And finally, we have Row Level Security
to control which resource can access which rows,
as well as Column Level Security to restrict access
to specific columns in your data sets for specific users.
So, that's it for all the technologies of use in security.
Could be boring, could be a lot of information,
but you do need to know all this information
going into the exam.
So, here it is to you.
Feel free to take your time, revisit them one by one,
read some blogs and get familiar with them.
All right, I hope you like this.
And I will see you in the next lecture.
