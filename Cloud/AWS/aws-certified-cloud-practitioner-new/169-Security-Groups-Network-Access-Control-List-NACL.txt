# 169. Security Groups & Network Access Control List (NACL)
# Section: VPC & Networking
# https://ua.udemy.com/course/aws-certified-cloud-practitioner-new/learn/lecture/20056244
# Caption: en_US (manual)

Now let's talk about network security
within our VPC.
So we have network ACL and security groups.
So if we look at our VPC
and look at a public subnet, we have an EC2 instance.
And the first line of defense
for our EC2 instance is a NACL
or a network ACL which is a firewall
that is controlling traffic from and to the subnet.
So remember it is at the subnet level
in which we can define, ALLOW or DENY rules
and then attach at the subnet level
and the rules can only include IP addresses.
So if we look at this diagram, as we can see,
the NACL is going to be filtering traffic in
and out of the subnet before it reaches our EC2 instance.
The second line of defense we have,
and we've been already using it so far in this course,
are security groups.
And this is a firewall that controls traffic
to and from an EC2 instance.
and the security groups can only have ALLOW rules
and finally they can reference either IP addresses
and other security groups.
So in this example, we have the security group
around our EC2 instance,
and that controls the traffic going in
and out of our EC2 instance.
So the security group and the NACL are quite different.
The NACL is at the subnet level
and the security group is at the EC2 instance level.
And from an exam perspective,
that should be all you need to know.
Now there is a more complete table around the differences
of the security group and the network ACL.
Let's look at the most important ones
and these are the first three,
while a security group operates at the instance level,
whereas a network ACL operates at the subnet level.
A security group supports only ALLOW rules,
whereas a network ACL supports ALLOW rules and DENY rules.
And finally in the security group,
the return traffic is automatically allowed,
regardless of any rules.
So that's called statefulness
and in network ACL
the return traffic must be explicitly allowed by rules,
which is called stateless, so that's it.
Again, from an exam perspective,
I really think the first two
or the first three rules are enough.
So let's go into our default DPC to see what we have.
So in our VPC on the left-hand side,
you can click on Security, then security groups.
And this will show a lot of the security groups actually all
that have been created during this course.
So you may see different ones, of course,
but you can actually see the security groups
either from the VPC console or from the EC2 console.
And if you click on one of those,
for example, launch-wizard-7,
you can look at the inbound rules
and you can see them, for example, this one.
So this is not something new,
but this is a way for us
to centralize all the VPC security groups
within this console.
What we haven't seen yet is network ACLs
on the left-hand side.
So as we can see here,
we have a ACL rule, a network ACL rule,
and it's already associated with three subnets.
So we are in the VPC ID, this one, which is our default VPC.
And so by default, a VPC will have a network ACL
associated with it, which is a default one,
and this one is associated actually at the subnet level.
So you can see three subnets are associated with it,
and you can click here to also view them in a separate page.
So this is very important because network ACL, remember,
are at the subnet level.
Now if we look at this default network ACL,
the one that's created by default,
we can see that for the inbound rules,
we have rule number 100,
which allows all traffic on all ports,
on all edges from anywhere to be allowed.
And then we have the DENY rule, which is the last one,
the star means last, and it says deny.
So the idea is that right now we take a look
at the first rule, this is this one,
and it's allowing everything on everywhere
and, therefore is going to be allowed.
So this is going to allow all traffic in,
and the outbound rule is very similar.
It's going to allow all traffic out.
So Which is what we would expect.
But if you wanted to, you could edit the inbound rules
and then you would add a new rule
and then you could say, for example, rule 200
and then you could select whatever you wanted.
For example, allow only HTTPS traffic from anywhere.
So if you had this rule
and you remove the first one, then if it's HTTPS traffic,
443 from anywhere that's going to be allowed,
else, everything is going to be denied.
So we don't want this right now
because we want to allow
a lot of different kind of traffics,
but this should could show you a way
to apply a firewall at this subnet level.
So that's it for this lecture.
I hope you liked it, and I will see you in the next lecture.
