# 370. SAM - Multiple Environments
# Section: AWS Serverless: SAM - Serverless Application Model
# https://ua.udemy.com/course/aws-certified-developer-associate-dva-c01/learn/lecture/43749230
# Caption: en_US (manual)

So SAM allows you as well
to easily manage multiple environments
within your development stack.
So imagine that you're deploying
sometimes your SAM templates into a
dev environment and sometimes into a prod environment.
So you as a developer, you have your SAM templates,
but you're also going to create another file named
"SAMconfig.toml".
And this file looks like this.
So it's in the TOML formats, and you actually have different
parameters for different stacks.
So here we have the dev stack,
and we define the deploy parameter.
So we type, we give a stack name,
we give an S3 bucket, prefix, a region, some capabilities,
and parameter overrides to say, hey,
the environment is development.
We can also set the sync parameters the same way.
And similarly, we can set the prod deploy parameters
by just starting by prod.deploy.parameters,
and again define the stack name, the S3 bucket,
the region, and so on.
And this time for the parameter overrides, we say, hey,
the environment is production.
And we can also specify some sync parameters.
So the reason why we do this is that once
we have done this initial setup in your TOML file,
then you can do "sam deploy",
and then "--config-env dev".
And automatically the SAM CLI is going
to look into your "Samconfig.toml" file,
is going to pick up the correct parameters
and automatically deploy resources
to the correct environment and the correct places.
And of course, you can change your command to say,
"sam deploy --config-env prod",
and you can define of course,
as many environments as you need.
So that's it.
It's something that can come up in the exam.
So I hope you liked it,
and I will see you in the next lecture.
