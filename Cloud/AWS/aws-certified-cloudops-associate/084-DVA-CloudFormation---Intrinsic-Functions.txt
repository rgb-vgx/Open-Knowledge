# 84. [DVA] CloudFormation - Intrinsic Functions
# Section: CloudFormation for CloudOps
# https://ua.udemy.com/course/aws-certified-cloudops-associate/learn/lecture/12905442
# Caption: en_US (manual)

So now let's talk about Intrinsic Functions.
So we've seen them before, but I wanna give you a list
and the ones that are in blue are the one you must know.
So we have the Ref,
the Get Attribute function,
the Fine in Map,
Import Value.
Join, Sub, For Each, Tojson String.
The Conditions Function such as If, Not, Equals, et cetera.
The Base 64, Cidr, Get Azs,
Select, Split, Transform, Length.
So all these are of course documented
on the CloudFormation website.
And so if one of them is not discussed in these slides
but you want to have a look at it,
please have a look online.
I'm going to discuss the most important ones,
especially from an exam perspective.
So the first one is the Ref function.
And so the Ref function, as you can see,
can be leveraged to get a reference.
It could be a reference to a parameter,
to return the value of this parameter.
Or to resources, to return the physical ID
of the underlying resource that has been created.
For example, an EC2 instance.
The shorthand is always with an exclamation point, so !Ref.
And here is an example of how to use a Ref.
So here we're creating a subnet
and we need to define the VPC to which the subnet belongs.
So we use the Ref to MyVPC
and MyVPC is either another resource
or it could be a parameter.
Then we have the GetAtt function.
So this is to get an attribute
and attributes are attached to any resource you create.
Now, to know what attributes a resource has,
the best place to look at is the documentation.
So let's do it right now.
So here I am in the AWS EC2 instance
CloudFormation documentation page
and I'm going to scroll down
and in the return values are here,
we have two options.
We have Ref and Fn::GetAtt.
So Ref is the instance ID that is going to be returned.
It's a reference.
And get attributes will give you some information
that is available about what has been created.
So we can get the ID of the instance,
but also the PrivateDnsName,
the Private Ip,
the PublicDnsName,
Public Ip and VpcIp.
This is all information that you can get directly
from this EC2 instance using the GetAtt function.
Please note you can only get what AWS chose to return.
You cannot get anything else
but these values that are documented in this page.
For example, we can get the public DNS name
of our EC2 instance.
So we create an EC2 instance using a CloudFormation template
and then we're going to create a record in Route53
to create a CNAME record.
So we need to have of course the public DNS name
of our EC2 instance to create this record.
So the way we do it is
that we use the get attribute function.
We give the EC2 instance a name,
a resource name within our confirmation template,
which is EC2 instance,
and then .PublicDnsName
to get the attribute public DNS name out
of this specific resource.
And then automatically it will be populated
for your DNS record on Route53.
So it shows you the power
of the get attribute function here.
We've seen Find in Map.
So again, it's to get a value directly from a specific key
in a specific map.
So this was used when we had mappings, of course.
We have also seen the ImportValue Intrinsic Function.
This was used to import values
that were exported in other stacks.
So here's an example in which we create an EC2 instance
and we import the value
of a security group ID named SSHSecurityGroup.
And that security group was defined in another stack
and of course exported.
We have the Base64 function
to convert a string into a Base64 representation.
And the reason why we do so, usually, is to pass data
to the UserData of an EC2 instance.
So we use the Base64 function
before we define the string of the UserData
and automatically it gets converted
and passed to your EC2 instance.
So that's probably one of the only usage of it.
We've seen the Condition Function.
So this is when we had a condition
to conditionally create resources and so on.
And so these Intrinsic Function can be used.
We have the And, the Equals, the If, the Not
and the Or functions.
Okay, so that's it for Intrinsic Function.
I hope you liked it and I will see you in the next lecture.
