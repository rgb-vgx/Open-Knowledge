# 226. MLOps with SageMaker, Kubernetes, SageMaker Projects, and SageMaker Pipelines
# Section: Machine Learning and AI Operations (MLOps/ AIOps) with AWS
# https://ua.udemy.com/course/aws-certified-machine-learning-engineer-associate-mla-c01/learn/lecture/45286939
# Caption: en_US (auto)

The field of MLOps or machine learning operations
really took off after the first iteration
of the AWS certified machine learning exam came out.
However, I expect the exam will catch up with this soon.
MLOps has really taken off in the meantime
and SageMaker has come up with a few answers
to that problem.
Basically, MLOps is how do I manage the larger pipeline,
the workflow that surrounds the building and testing
and deployment of my machine learning models.
We've talked about some individual systems
that handle that already within SageMaker,
but sometimes people need to do something more complicated
or have more direct control,
or they might need to integrate with existing pipelines.
One of those existing pipelines might be Kubernetes.
So if you already have an existing Kubernetes-based
machine learning infrastructure,
you might want a way to integrate SageMaker into that.
And then there's components for Kubeflow pipelines,
Kubeflow being the Kubernetes answer for MLOps
that allows you to define these pipelines of events
that happen and can, for example,
wrap the entire process of building and deploying
and testing and tuning your models in an MLOps environment.
This enables a hybrid machine learning workflow.
So if you have an existing Kubernetes
machine learning workflow that runs on-premises,
this might be a way to integrate SageMaker
into it gradually over time.
So if you're not ready to go all in with SageMaker
and you have an existing MLOps pipeline with Kubernetes,
this offers a way to integrate the two
and move over more slowly, right?
So that's kind of the idea.
Whether you're built on Kubernetes or Kubeflow,
this offers a way to integrate existing platforms with SageMaker.
And even if you might want to even keep it that way, right?
So like sometimes you want to keep some stuff on-premises.
Maybe you're processing some sensitive data
that just can't go in the cloud.
This would offer a way to integrate the two.
Then we have SageMaker components for Kubeflow pipelines.
Again, this is just integrating SageMaker with Kubeflow,
MLOps solution under Kubernetes.
And with SageMaker components for Kubeflow pipelines,
there are separate components available for processing,
like that might be wrapping a Spark container.
There's a process for hyperparameter of tuning
and hyperparameter optimization.
There's a component for training in SageMaker
and there's a component for inference,
actually hosting your model in SageMaker as well.
This can all be integrated into your Kubeflow pipeline
by using SageMaker components.
Now, what if you don't want to use Kubeflow or Kubernetes?
So if you just want to like stick with SageMaker,
you're starting from scratch,
you're all in with SageMaker, right?
Obviously there's a way to do MLOps without Kubernetes
and just using SageMaker.
That's what SageMaker projects are for.
So this is SageMaker Studio's
native machine learning operation solution
with continuous integration and continuous deployment.
It allows you to build these pipelines
that will let you build images, prepare your data,
do feature engineering, train your models,
evaluate your models, deploy your models,
monitor and update those models, whatever you need to do.
You can create these complex workflows
as part of a SageMaker project.
Typically that will use a code repository
of some sort integrated into your project
that will be used for building
and deploying your machine learning solutions.
And it will use something called SageMaker pipelines
that actually define those steps.
So in this example here,
you can see we set up a SageMaker pipeline
that defines the process of processing your data,
training your model, evaluating your model,
and then registering it into a model registry
for later deployment.
And that model might get stored off into S3, for example.
So two key terms here, SageMaker projects,
that's basically MLOps for SageMaker,
and SageMaker pipelines,
that's the way to chain these different steps together
within SageMaker.
Taking a quick look at a larger example here
of a SageMaker project,
you can see you can do some pretty complicated stuff here.
So you might have somebody committing changes to a model
and that model building code
might trigger an Amazon EventBridge notification.
That in turn might kick off AWS code pipeline
to actually build that model.
And then code build would build and execute
the actual SageMaker pipeline
that we talked about in the previous slide.
That SageMaker pipeline would then do its thing
to process your data, train the model,
evaluate the model and register it to the model registry.
At that point, we might kick off another event
to Amazon EventBridge,
and that might in turn create,
sit on top of another AWS code pipeline.
This one intended to deploy the model.
That could kick off a code build for CloudFormation
to deploy your endpoint,
which in turn could go off to AWS CloudFormation
and actually kick off your SageMaker endpoint there for staging.
It could then test that staging endpoint using code build.
If that's approved manually,
then CloudFormation could be told to deploy it to production
again using SageMaker hosting.
This is a more complex example of MLOps in general,
and specifically what SageMaker projects
can enable you to do.
