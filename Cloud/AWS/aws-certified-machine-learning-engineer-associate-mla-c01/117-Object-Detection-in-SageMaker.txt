# 117. Object Detection in SageMaker
# Section: SageMaker Built-In Algorithms
# https://ua.udemy.com/course/aws-certified-machine-learning-engineer-associate-mla-c01/learn/lecture/45285491
# Caption: en_US (auto)

Let's talk about a fun one the object detection algorithms in SageMaker.
The idea here is to identify all of the objects in an image, along with where they are, with bounding
boxes of where those objects exist.
So in this example to the right here we have a picture of a little office here.
And it's telling you, hey, someone had a bad day.
They got a wine bottle there and a wine glass.
And it tells you where those are in the image and what the bounding rectangles are around them.
Also, there's a chair, a laptop, a bowl.
So this is what it does for you.
It detects and classifies the objects within an image using a deep neural network.
And those classes come back to you with confidence scores as well.
So you can see just how sure it is that that's a wine glass as opposed to something else.
You can either train these from scratch or use pre-trained models based on ImageNet.
In the case of MXNet.
Or there's also a TensorFlow variant where you have more choices.
So yeah, let's talk about that.
Technically there are two different object detection algorithms in TensorFlow.
One is object detection MXNet, which I believe is the older original one.
And then there's object detection TensorFlow, which gives you some more choices.
In both cases, they'll take an image as the input and output all the instances of objects in the image,
with categories and confidence scores associated with them.
Now the MXNet version uses a convolutional neural network under the hood using the single shot multiplex
decoder algorithm SSD, and under the hood that CNN can be Vgg16 or ResNet 50.
It has both transfer learning modes and incremental training modes, so you can use a pre-trained model
for the base network weights instead of random initial weights if you need to.
Internally, it uses flip, rescale, and jitter to avoid overfitting.
Then we have the TensorFlow version object detection TensorFlow that allows you to choose from many
different models.
There are various incarnations of ResNet Efficientnet MobileNet to choose from there from the TensorFlow
model garden, and the details of how to train those and how to use them and how to configure them will
vary based on the model you choose.
It's kind of a a wrapper, if you will, for TensorFlow model garden models.
They're now in the case of MXNet, since we do know what model it's using, it is more specific about
its training input.
For that case, you want to use MXNet Recordio format or an image format, JPEG or PNG.
And with that image format, you'll supply a JSON file for annotation data for each image when you're
training it.
So this is the case where you're training object detection with your own data about what objects are
in what images.
So you'd give it a file that looks like this that says, hey, here's a picture that has stuff in it.
Here's the size of that image.
Here are the annotations of what's in it.
Here are the categories of that area and so forth.
In the case of TensorFlow though, um, it will vary based on what model you select.
There are some important hyperparameters for both MXNet and TensorFlow, and MXNet is called Minibatch
size TensorFlow.
TensorFlow just calls it batch size.
Same thing.
You know, standard deep learning stuff of tuning the batch size that you're training with.
There's also learning rate.
And you can choose any one of a number of optimizers whatever works best for your data.
As far as instance type recommendations go, this is an intensive operation for training.
You want to use GPU instances, and you can use a multi-GPU instance and even multiple GPU instances
if you need to.
It works.
In any of those cases, they recommend the P2 Xlarge or P3 two Xlarge instance types, and if you need
more, you can go up to 16 Xlarge on either of those classes.
Also, the newer G4 and G5 instance types are on the table as well.
On the inference side, you can go GPU or CPU.
They recommend M5.
On the CPU side and on the GPU side, anything goes.
P2, P3, G4 are all okay.
