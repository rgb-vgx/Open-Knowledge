# 130. Deep Learning History and Overview
# Section: Model Training, Tuning, and Evaluation
# https://ua.udemy.com/course/aws-certified-machine-learning-engineer-associate-mla-c01/learn/lecture/56466801
# Caption: en_US (auto)

So, generally speaking, natural language processing is implemented using deep neural networks
and very specific kinds of them.
We'll get into more details.
But let's talk briefly about the history of deep neural networks and how these ideas came to be.
It's pretty amazing stuff.
So, the whole field of artificial intelligence is based on this understanding of how our
own brains work.
We've kind of diverged from that lately, but this is where it got its roots, just trying
to understand how do our actual brains work, how our neurons, our biological neurons functioning
inside our heads.
And over millions of years of evolution, nature's come up with a way to make us think.
And if we just reverse engineer the way that our brains work, maybe we can figure out how
to make machines that think.
That's how we started off.
Now within your brain, specifically within your cerebral cortex, that's where all your
thinking happens, you've got a bunch of neurons, right?
These are individual nerve cells and they're connected to each other via axons and dendrites.
You can think of those as connections or wires, if you will, that connect different axons together.
Now an individual neuron will fire or send a signal to all the neurons that are connected
to it when enough of its input signals are activated.
At the individual neuron level, it's a very simple mechanism.
You just have this neuron that has a bunch of input signals coming into it, and if enough
of those input signals reach a given threshold, it in turn will fire off a set of signals
to the neurons that it in turn is connected to as well.
But when you start to have many, many, many of these neurons connected together in many
complicated ways with different strengths between each connection, things get pretty
complicated, right?
And this is a perfect example of what we call emergent behavior.
You have a very simple concept, a very simple model, but when you stack enough of them together,
you can create a very complex behavior that can yield learning behavior.
And this actually works.
So not only does it work in your brain, it also works in our computers as well.
Now think about the scale of your brain.
You have billions of neurons and each of them have thousands of connections.
And that's what it takes to actually create a human mind.
Now this is a scale that we're just starting to approach in the field of deep learning
and artificial intelligence, but it's the same basic concept.
You have a bunch of neurons with a bunch of connections that individually behave very simply.
But once you get enough of them together and they're wired enough in enough complex ways,
you can actually create very complex thoughts, even maybe consciousness.
So the plasticity of your brain is basically tuning where those connections go to and how
strong each one is.
And that's where all the magic happens inside your own head.
Now, furthermore, if you look more deeply into the biology of your brain, you see that
within your cerebral cortex, neurons seem to be arranged into these stacks or cortical
columns that process information in parallel.
So, for example, in your visual cortex, different areas of what you see might be getting
processed in parallel by different columns or cortical columns of neurons, as it were.
Each one of those columns, in turn, is made up of many columns of around 100 neurons per mini column.
Now, many columns are then organized into larger hyper columns.
And within your cortex, there are about 100 million of those many columns.
So, again, they just add up quickly.
Now, coincidentally, this is a similar architecture to how 3D video cards in your computer work.
It has a bunch of very simple, very small processing units that are responsible for
computing how little groups of pixels on your screen are computed.
So it just so happens that's a very useful architecture for mimicking how your brain
works. So that same parallel architecture that went into GPUs for doing computer graphics
can be used for artificial intelligence as well.
And that's why NVIDIA has come to take over the world in AI, right?
Because they happen to stumble upon an architecture that was originally built for
computer graphics, but turned out to be useful for artificial intelligence as well.
Now we have more specialized hardware that's geared toward artificial intelligence more
than graphics, of course, but that's where it came from.
But it's just sort of a happy accident that the research behind your favorite video game
lent itself to the same technology that made AI possible in a grand scale.
So let's get into the history of artificial neurons.
This goes all the way back to 1943 people, believe it or not.
So at one point somebody said, hey, the way neurons work or the way we think they work is
actually pretty simple.
Wouldn't be hard to replicate that ourselves and try to build our own brain.
And again, this goes all the way back to 1943.
So people proposed a very simple architecture where if you have an artificial neuron, maybe
you can set up an architecture where that neuron fires only if there's a certain number of
input connections that are active.
So when they thought about this more deeply in a computer science context, people realized
you can actually create logical expressions or Boolean expressions from doing this.
So depending on the number of connections coming in from each input neuron and whether
each connection activates or suppresses a neuron, you can actually implement simple logical
expressions in artificial or natural neurons.
So this particular diagram is implementing an OR operation.
So imagine that our threshold for our neuron was that if you have two or more inputs active,
you will in turn fire off a signal.
So in this setup here, we have two connections to neuron A and two connections coming in
from neuron B. Now, if either one of those neurons were to produce an input signal, that
would cause neuron C to fire.
So you can see we've created an OR relationship here where if either neuron A or neuron B
feeds neuron C two input signals, then that will cause neuron C to fire and produce a true
output. So we've implemented here the Boolean operation C equals A or B, just using the
same wiring that happens within your own brain.
And it's also possible to implement AND and NOT operations in similar terms.
So then we start to build upon that idea.
So we created something called the Linear Threshold Unit or LTU for short, back in 1957.
AI is not new people at all.
So this just built on things that assigned weights to those inputs.
So instead of just a simple on or off switch, we now have the concept of having weights on
each of those inputs as well.
So this is working more toward our understanding of the biology.
Different connections between different neurons may have different strengths.
And we can model those strengths in terms of the weights on each input coming into our
artificial neuron.
We're also going to have the output be given by what we call a step function.
So this is similar in spirit to how we were doing it before.
But instead of saying we're going to fire if a certain number of inputs are active, well,
there's no concept anymore of active or not active.
There's just weights coming in.
And those weights could be positive or negative.
If the sum of those weights is greater than zero, then we'll go ahead and fire off a signal.
If not, if it's less than zero, then we don't do anything.
So it's just a slight adaptation of the concept of an artificial neuron where we're
introducing weights instead of just a simple binary on or off switch.
So let's build upon this even further and create something that we call the perceptron.
Now, a perceptron is just a layer of multiple linear threshold units.
So now we're starting to get into things that can actually learn.
By reinforcing the weights between the inputs, we can actually build a neural network
between these LTUs that produce the behavior we want.
We can create a system that learns over time to produce the desired output.
This is also working more toward growing our understanding of how the brain works.
So within the field of neuroscience, there's a saying that cells that fire together wire
together. And that's speaking to the learning mechanism going on in our artificial
perceptron, where we have weights that are leading to the desired result that we want.
We can think of those weights as strengths of connections between the neurons, and we can
reinforce those weights over time and reward the connections that produce the behavior that we
want. So you see here, we have our inputs coming into weights just like we did in LTUs
before. But now we have multiple LTUs grouped together in a layer.
Each one of those inputs gets wired to an individual neuron in that layer.
We then apply a step function to each one, which will produce a final set of outputs that can
be used to classify something like what kind of image is this perceptron looking at?
Is it an apple? Is it a pear? Is it a banana?
Right. Another thing we introduce here is something called the bias neuron off there on the
right. Now, it's something to make the mathematics work out.
Sometimes we need to add in a little fixed constant value to make the neurons fire at the right
values. And this bias amount can also be learned as the perceptron is trained.
So this is a perceptron.
We've taken our artificial neural network, moved that to a linear threshold unit, and now we've put
multiple linear threshold units together in a layer to create a perceptron.
We now have a system that can learn as you optimize all of the weights between those neurons in
each layer. And you can see there's a lot of those weights at this point, which can capture fairly
complex information.
So if you have one of those inputs going to every single LTU in your layer, they add up fast, and
that's where the complexity of deep learning comes in.
So let's take that one step further and we'll have a multi-layer perceptron.
So now instead of a single layer perceptron of LTUs, we're going to have more than one.
We actually have now a hidden layer in the middle there.
So you can see that our inputs are going into a layer at the bottom.
The outputs are layered at the top.
And in between, we have this hidden layer of additional LTUs, linear threshold units, right?
And that can perform what we call deep learning.
So here we have already what we would call today a deep neural network.
Now, there are challenges of training these things because they are more complex.
But the thing to really appreciate here is just how many connections there are.
So even though we only have a handful of artificial neurons, you can see there's a lot of
connections between them and a lot of opportunity for optimizing the weights between each
connection. So that's how a multi-layer perceptron works, an MLP.
You can just see, again, that we have emergent behavior going on here.
An individual linear threshold unit is a pretty simple concept.
But when you put them together in multiple layers, all wired together, you can get very
complex behavior because there's a lot of different possibilities for all the weights
between those different connections.
Finally, we'll talk about a modern deep neural network.
And really, this is all there is to it.
So we're just going to be talking about specific ways of implementing this going forward.
All we've done here is replace the step function with something better.
So in this case, we're illustrating something called a ReLU, R-E-L-U.
The key point there is that the step function has kind of a nasty mathematical properties.
So especially when you're trying to figure out slopes and their derivatives, which is how
you train these things. And I'm not going to get into all the calculus of how gradient
descent works when you're training a neural network, because there's no way the exam is
going to expect you to know that stuff.
But the point is just that instead of a simple step function, different activation functions
work better mathematically.
So don't worry about it too much.
Now, we're also going to apply something called softmax to the output at the top there.
And that's just a way of converting the final outputs of our neural network into probabilities
of a given classification.
So maybe those top three neurons coming out of our system means I think it's an apple or a
pear or a banana.
Softmax just converts those output values into actual probabilities for each one of those
classifications. So to make a choice, we just pick the one with the highest odds and say,
OK, this is what I think it is.
All right. So in the past five minutes or so we've been talking, I've given you the entire
history of deep neural networks and deep learning up to a point.
So it's not really that complicated, right?
That's the beauty of it. It's emergent behavior.
So you have very simple building blocks.
But when you put these building blocks together in interesting ways, very complex and
sometimes mysterious things can happen.
So let's tie this back into natural language processing.
So here's what a modern deep neural network might look like conceptually at a very high
level. So imagine individual words are being fed into this neural network to the left space,
the final. And the purpose of this neural network is to predict the next word in that sentence,
in this case, frontier.
So this is all large language models really do.
There's just this mess of neurons there and they're arranged in a very specific way and doing
very specific things.
We'll get into that. But at the end of the day, it's a giant deep learning neural network that
predicts the next word in a sentence.
So the input is just the words coming in and those get converted into numbers that represent
tokens, whatever. We'll get into the details later, but that distills things down to what we
want. So we feed those input words into an interconnected mass of artificial neurons that
combine the signals from that layer to their left.
And that's distilled down to the thing that we want to predict, such as the next word in the
sentence. Maybe it's something like, is this sentence a positive or negative sentiment?
Maybe it's a classification problem, whatever it might be.
A deep neural network is a very flexible thing that can do many different things.
Each line here represents a weight attached to that connection between neurons.
And learning those weights is how these things work.
So deep learning just means there's more than one layer here.
And that's almost always the case these days.
Now, during training, the way it works is that we start with a bunch of random weights between
those connections. And if they start to produce the correct result that we want at the end of the
day, then we start to reward the weights that led to that correct result during training.
I'm oversimplifying, but that's the general idea.
So they really work by just having a massive amount of trial and error over time and
learning as it goes over what random actions led to the correct result and reinforcing those
actions during the training process.
Now, that training obviously can be very expensive and time consuming because it is just
basically trial and error at the beginning.
And it takes a while to get that model to a point where it's actually doing the right thing
reliably. But at a high level, that's deep learning and how it works and how it ties back to
natural language processing.
Let's dive into some more specific architectures of deep learning next.
