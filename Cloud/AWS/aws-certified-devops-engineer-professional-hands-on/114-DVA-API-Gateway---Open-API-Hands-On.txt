# 114. [DVA] API Gateway - Open API Hands On
# Section: Domain 3: Resilient Cloud Solutions
# https://ua.udemy.com/course/aws-certified-devops-engineer-professional-hands-on/learn/lecture/37903894
# Caption: en_US (manual)

So let's go and see how we can first import
an OpenAPI definition.
So I'm going to create a new API.
It's going to be a REST API, and I can click on imports.
And when you import an API, you need to just
have an API definition file in the OpenAPI format.
And if you click on Example API this is one
of these example.
So this defines actually how an API should be built
on the API Gateway.
And so if you just import this example API,
and click on Create API.
As you can see we have a new API called Pet Store,
that has been created which contains the proper resources
that we need to.
And this is very handy because all of this came
just from an OpenAPI definition file.
So we see how we can import one but how can we export
our current API as an OpenAPI?
So we can do is that we go to API.
Let's go back to my first API.
Go to a stage, for example, the prod stage.
And in stage action you can export it, so if you export it,
you specify the format you want, is it swagger
or OpenAPI 3, is it JSON or YAML?
And then do you want to have the extensions
of OpenAPI gateway and Postman?
So lots of options.
And when you to export API, this generates the file for you
that you can then import somewhere else if you wanted to.
And on top of it, because you're using the OpenAPI format,
then you can automatically generate SDKs,
for your programming languages.
For example, if you have Android or JavaScript or iOS,
Java or Ruby you can generate an SDK directly
from the API Gateway.
And then your applications can very easily interact
with your API through this SDK.
So this really shows the power of OpenAPI definitions
in API Gateway.
I hope you liked it and I will see you in the next lecture.
