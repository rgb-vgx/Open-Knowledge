# 94. SSM Hybrid Environments
# Section: Domain 2: Configuration Management & IaC
# https://ua.udemy.com/course/aws-certified-devops-engineer-professional-hands-on/learn/lecture/37869568
# Caption: en_US (manual)

So, as you know,
Systems Manager can also be used
to manage on-premises servers,
and also, IoT devices, edge devices, and virtual machines.
And so, to do so, we will have them
registered into Systems Manager,
and so, the EC2 instances will have the prefix I,
as in I and then the EC2 instance ID.
And then when you have a hybrid activation
you will have a managed node with a prefix mi.
So the way it works is that we want to register
for example, a server from an on-premises data center.
We're going to create a hybrid activation onto systems
manager and get back an activation code and ID from it.
From there, we install the SSM agent onto the target server
and then we register the SSM agent
with the systems manager service
by using this activation code and ID obtained previously.
And then we'll have a manage instance showing up as mi
and then the instance ID.
So on top of it
you can automate this by using an architecture
by leveraging the API Gateway, Lambda and systems manager.
So the server upon booting up
will do a get request into an API gateway.
The payload will be sent to Lambda
which will use API calls to create a hybrid activation
on systems manager.
Get back the activation code and ID, pass it back
through the API gateway and then to the server
which will then be able to register itself.
And the script to allow the server to do this
automatically is displayed here.
But you don't need to remember this
just the little diagram I showed you above.
Okay, that's it.
I hope you liked it and I will see you in the next lecture.
