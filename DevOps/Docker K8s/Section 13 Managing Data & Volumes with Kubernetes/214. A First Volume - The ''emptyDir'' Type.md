Remember what I mentioned

about the volume lifetime,

it depends on a pod because volumes are attached to pods.

And the data, of course,

is what I already mentioned a couple of sections ago.

So volumes are really attached to pods

and are pod specific.

And that, of course, has one important implication.

We have to define volumes

in the place where we define and configure a pod.

So in our case, in the deployment yaml file.

Here, we get the specification for our pod,

which should be part of that deployment.

And therefore it's here in this pod specification,

where we also need to define our volume.

Now, in order to see whether

that volume really has an effect,

I am going to do something though,

I am going to add an error route here,

slash error to app.js, simply by adding app get slash error.

And in there, in this function,

which have passed to this route,

which is therefore executed when this route is reached,

I will call process dot exit with a code of one.

And this will crash the container and cause it to restart.

And we need this now

to really see that volumes make a difference.

So let's add this route here.

Thereafter, run the docker build command again,

and it will give this a tag of one now.

So I add colon one here.

And of course there after push it

also with that tag off colon one at the end,

like this.

So push this new image,

which now has this new code inside of it.

And then let's go to our deployment yaml file,

and also use this tag here

to ensure that this specific version of the image

is pulled in.

And now we can run

kubectl control apply dash f equals deployment dot yaml.

With that, we're applying this new configuration.

And therefore of course, this pod will now be recreated

with this new container

based on this new image inside of it.

Hence, if I run get pods here,

at the moment, we see that one pod is terminating,

and the new one is already up and running.

So if we now send the get request here,

of course, our data is lost,

because now because of this image change,

the container was restarted.

And actually even a new pod was started.

But if I now store this again,

and reload, we got our data back.

But now with this error route added,

if we send the request to that,

so if we use our service URL,

and we send the request to slash error,

this crashes everything.

And if we send the get request thereafter,

we see it works, because the container was restarted,

thanks to Kubernetes.

But our data is gone, it's not there anymore.

And the reason for this, of course,

is that the container restarted, not the pod.

However, if we get our pods,

we still see it's one running container,

which simply was restarted once.

So now we saw the problem in action.

And now let's use our volume to fix that problem

and to see a difference.

And for this, since volumes are attached to pods,

here on this pod specification,

next to containers,

so not nested inside of containers.

But on the same level as containers,

we add a volumes key.

And here we can define all the volumes

which should be part of this pod.

And all containers in that pod

will then be able to use that volume.

Now volumes is a list of volumes.

So we need a dash here.

And then we give every volume a name.

Now this name is totally up to you.

I'll name it story volume,

because it's there to store our stories.

Now besides the name on the same level as name,

we now need to set up the volume type.

And there we have this long list of available types.

And depending on which type you choose,

you then have more options which you can set for this type.

Here we're going to use the emptyDir type,

written like this, small e at the beginning,

uppercase D in there, all one word.

And you then can set this to an empty object like this.

This means that you don't have

any specific configuration for it,

but that you just want to use this type as it is

with all its default settings which typically suffice.

Now what does this then do?

emptyDir simply creates a new empty directory

whenever the pod starts.

And it keeps this directory alive

and filled with data as long as the pod is alive.

Containers can then write to this directory.

And if containers restart or are removed,

the data survives.

But if the pod should be removed,

this directory is removed.

And when the pod is recreated,

a new empty directory is created. That's the idea here.

And that is a good start.

Because this gives us a directory

which survives container restarts.

And the data in there once written into it,

will also survive.

Now we set up the volume.

But now we also need to bind it into a container.

So we need to make it available inside of a container.

And we do that in the container configuration.

So where we also set up the image for the container,

for example, there, we now add a volume mounts option.

So we first define the available volumes.

Now inside of a container,

we define where which volume should be mounted.

And we do that with volume mounts.

This again takes a list.

And then here we need two important options.

The first one is the mountPath.

This is now the container internal path

where this should be mounted.

And of course, this depends on your application,

and where it's going to write data.

This application here is going to write data

in a story folder.

And it's doing this

inside of the project folder of this code file.

And thereafter in the working directory set up by the image.

So here, it's slash app slash story.

Slash app is simply my working directory

as to find the Dockerfile.

And then in there,

we're accessing a story folder

where then this text file is created.

So this is my path into container

where this empty directory should be mounted in.

So that's the thing you would also define

in a docker-compose file.

And indeed I did, you see slash app slash story.

That's the path I defined here for this named volume.

The only difference now,

is that we don't have a named automatically created volume,

but that we tell Kubernetes,

which type of volume should be created

by adding this driver, this emptyDir type here.

Now mountPath this one important thing.

But of course, you could have multiple volumes

to find down there for different containers, for example

Or also because maybe one container

once use different volumes and different volume types

for different container internal paths.

That's why besides the mountPath,

you also need a name key here.

And there, you now point at the volume name

you wanna use for this container internal path.

Of course, here we have only one volume,

but we could have more.

So here we repeat this one volume name, which we got.

So that this volume here,

this one volume defined here,

is mounted into this path into container.

And this should now make a difference.

So, let's again, apply this changed deployment yaml file,

which now of course, again, terminates the old part

and starts a new one.

But now, our data should survive container restarts.

So in postman, if we get our data,

of course, it's empty again.

But actually see, I get a different error,

I get failed to open file.

And the reason for that is that in my source code,

I tried to access a text dot txt file.

But this file doesn't exist initially,

in this empty directory,

because it is just that it's an empty directory.

So the file doesn't exist yet. Hence, reading from it fails.

It's not necessarily a problem, though.

Writing to it will succeed

because the file is simply created if it's missing.

So let's try to post request, and we get text was stored.

And if we now try to get the data,

indeed, we see we do get it.

And if we send multiple post requests,

you see that data is simply upended.

Now the interesting thing of course is

what will happen if we run this error request again.

And if I send this and therefore the container restarts,

you now see if I sent the get request again,

the data is still there,

even though the container was restarted,

because now we're using a volume.

And that's the difference compared to before.


