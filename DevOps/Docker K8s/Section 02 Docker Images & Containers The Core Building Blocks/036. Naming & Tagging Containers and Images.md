Now the last options, which you can set

when you create an image or a container,

which I wanna explore with you for now,

are the options for naming images and containers.

Thus far, if we for example list the images,

we always work with the image IDs here

because we didn't set our own name or tag

as it's named here.

You can tag images and you'll see that

the node image actually has a tag and a repository

but both is none here for our own image.

And for containers it's quite similar.

If I run a container again, like this,

and I then list all my containers,

you see this also has just this automatically

generated name here.

That's of course better than nothing,

but you often have to look it up and copy it manually

because you might not remember it,

or it also might be quite long and annoying to type.

That's why you can give containers your own names

and why you can also add your own tags to images.

Now let's start with containers.

I'll actually stop this container here

and once it is stopped, I wanna run it again

but now with my own name provided to it.

So let's wait for that stopping to, well finish.

And then let's first of all,

look into the help options or the help screen

of the Docker run command again.

And we have a lot of options here

but the one option is to add a name

with dash dash name to the container.

And that's exactly what we need here.

If I rerun my container here,

I can add dash dash name

and now provide any name of your choice.

Like for example goalsapp

because my node app again

is about showing me this screen

that allows me to set a goal.

If I now run this, in detached mode

with the auto removal and everything we saw before,

if we list our containers,

now this container has our custom name

and the advantage of this, is that we now

probably don't need to list this all the time,

instead since we assigned our own name,

it's probably something we can keep in mind.

So therefore, stopping it might be a bit easier

because we don't have to list all containers

and copy the name first,

instead, we assigned our own name,

therefore we probably memorized it

and we can just use it like this.

So, that might be a bit more convenient now.

Now a similar concept exists for images.

Here we got our two images

and if I now would build a new image again

with Docker build dot, based on this Dockerfile,

we would of course create that,

but it's just having this automatically generated ID.

So if I list my images, we have this new image here

but it has just this ID.

Well you can also assign a name so to say to images

but there it's called a tag.

And actually image tags consists of two parts.

It's the actual name, also called repository of your image

and then a tag separated by a colon.

This concept of having these two parts

exists for a simple reason though.

With the name you can set up

the general name of your image.

To be precise you can create a group of multiple,

maybe more specialized images.

For example node, the image we pulled in

but then with the tag and that's optional, as I said,

you can define a more specialized version of that image.

Within that group of images and we haven't seen this

in action yet, but for node for example,

we don't just have to pull in the node image like this,

we could also add a tag here

and say that we want node version 14 or version 12.

And if we check out the note page on Docker Hub,

we see that they support a lot of tags.

These here are all the tags you can set after the colon

after the word node.

So here's the 14 tag for example,

further down we see the 12 tag

and anything, any of these words here

can be used as a tag to tell node,

or to tell Docker Hub and Docker therefore,

that you wanna use a specific version

of the node image.

It's all about Node.js being included the image

but with the tag in nodes case,

we can use a specific version,

a specific configuration, for example

an extra slim configuration, which is a bit smaller

because it uses a more lightweight operating system

under the node and so on.

You can really turn this into a philosophy

but when you're just starting, you don't have to.

You can simply use just node

or maybe combine it with a version

because maybe for example we know,

that the host we wanna run this application on

will only support node version 10 and 12,

well then we might wanna use 12 as a tag here

to use the version 12 version of this node image.

So that's the idea behind names and tags.

Node is the general group of images

but then we got specialized images

which have different tags,

which allow us to, well use exactly the version

or the configuration of node, which we need

in our own image here.

Well, and we can do the same with our own images.

We can give them a name but then if we would have

the scenario that we have different versions

of that image or different configurations, whatever,

we could also combine our name with a tag.

And combined you'll always have

a unique identifier for a very specialized

version of an image.

Of course, if an image has no tag,

the name alone is also already a unique identifier.

So that was a lot of talking,

how do we now specify a tag,

well do this when we build an image.

Here, if we use dash dash help,

we also got a bunch of options, a lot of options

which we will rarely need

but we get the dash key option to add a name and a tag

in this name colon tag format.

So now we could Docker build

our Dockerfile in this directory

but we can add dash T in front of the dot

and then specify our name tag combination.

And here we could give this a name of goals

and maybe a tag of latest,

to indicate that this is the latest version

of the goalsapp

but both is totally up to you

the name as well as the tag.

The tag can be a word, the tag can be a number

like version one, two, three

if you wanted that, totally up to you.

Here I'm going for goals latest

and if I'm doing that, it now needs to redownload 12

because I switched to node 12 here,

by adding the 12 tag,

therefore it rebuilds the entire image

but once it is done,

you see that if I list all images,

now this image here has that name or repository

as I mentioned of goals and that tag of latest.

And we could now rebuild it again,

maybe with a slightly different Dockerfile

and use goals 2 or goals whatever.

So that's the flexibility you have there

and that's how you can also name your images.

And now if you want to run your app based on that image,

you can also execute the Docker run command,

not just on that image ID

but also on that tag, goals latest for example.

You can plug in that tag here instead of the image ID,

and this will now start a container

based on this image with that tag,

with that name tag combination which I provided here.

So now let me stop that again though,

since I assigned my own name,

I can use that for stopping

and that's what you should know about names and tag,

why we have them and especially in the case of images,

how tags work, that with tags you can have

multiple, more specialized version of a given image.

Like here with the node image,

where we then have a lot of specialized versions

of that node image, which can be identified

by adding the proper tag, after the image name.