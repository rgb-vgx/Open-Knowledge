Hi, this is Stephane from Conduktor.

And now, we're going to talk

about the log cleanup policy called compact.

So this is log compaction.

And log compaction means that your log contains

at least the last known value

for a specific key within a partition.

I will show you a diagram

that will make a lot of sense for you.

So log compaction is very helpful

when you just require a snapshots of the current state

of a topic with the latest value of each key

instead of the full history which is...

For example, log compaction

is similar to data table and database

where you just have, well, the state of the table right now,

but you don't know all the changes

that happen to your table over time.

So the idea is that we want to only keep the latest update

for a key within our log.

So here's an example that will make it concrete.

Let's take an employee salary topic,

and you want to keep only the most recent salary

for our employees, and what happens?

Well, here is the order or writes from top to bottom.

So we first write John.

John has a salary of 80,000.

He's our first employee.

Mark has a salary of 90,000.

He's our second employee.

And then Lisa is our third employee.

She has a salary of 95,000.

So at some point the segment is closed,

and this becomes the first segment.

More writes happened over time.

And then at some point, Lisa gets a promotion.

Now, her salary is 110,000.

And then John gets a promotion,

his salary is 100,000.

And so that makes it up for segment 2.

So then happens log compaction

if we enable it, of course, on our topic.

And after compaction,

we're going to have one new segment

created from both previous segments.

And on it, Mark will be retained

because this is the latest occurrence of Mark.

His salary has not changed, so it'll stay 90,000.

Then Lisa will have the latest key available,

so 110 as the salary.

And then John will have a salary of 100,000,

this is the latest salary.

But as you can see,

your data from before, for offset 0 and offset 2,

have been deleted because John has a newer salary

and Lisa has a newer salary.

So log compaction does not change the offsets,

it just deletes keys

when there are newer keys available in the topic.

So some guarantees about log compaction

is that any consumer reading from the tail of a log,

which is the most current data,

is still going to see all the messages

sent to the topic in order.

Also, even after log compaction,

the ordering of messages is kept.

And log compaction only removes some messages,

but does not reorder them.

The offset of a message as well does not change.

It's immutable.

So in case an offset has been deleted,

it's just skipped by the consumer if a message is missing.

And deleted records in your log from log compaction

can still be seen by consumers with a grace period

of delete retention millisecond,

which by default is 24 hours.

So here's some myth busting about log compaction.

One, it does not prevent you

from pushing duplicate data into Kafka,

because the de-duplication, quote, unquote,

the fact of just removing keys over time,

is done only after a segment is committed.

So your consumers reading from the tail of the topic,

we'll still read the data as soon as it arrives.

Also, it doesn't prevent you

from reading duplicate data from Kafka

because of the points from the above.

And then finally, log compaction can fail from time to time.

It's an optimization,

so check it out and restart Kafka

if log compaction is broken.

And then right now,

we cannot trigger log compaction using the API call,

but maybe they will change in the future.

So how does it work for log compaction?

Well, let's imagine we have a topic with multiple segments.

So three segments and then an active segments.

In log compaction,

it's going to take all the previous segments

and rewrite them as new segments.

So the setting log.cleanup.policy=compact

is going to be impacted by other settings.

The first one is segment millisecond, by default seven days,

which is the maximum amount of time to wait

to close an active segment.

Segment.bytes which is one gigabyte,

which is the max size of a segment,

and again, this is how long to wait until we close it.

Min compaction lag millisecond, default value is zero,

which is how long to wait before a message can be compacted.

And delete retention millisecond, 24 hours,

we said it before, which is how long to wait

before data marked for compaction is deleted.

And then finally, min.cleanable.dirty.ratio,

which by default is 0.5,

but a higher ratio means less, more efficient cleaning

and a lower ratio means more cleaning but less efficient.

Okay, so we've seen the basics

and the theory of log compaction.

I will see you in the next lecture for some practice.
