So let's talk about the first log cleaner policy

that is the default one called deletes.

So we have two settings that will impact this

log cleaner policy.

The first one is log retention hours, which is a number

of hours to keep your data for,

by default it's 168 hours

which is exactly one week.

So if you do a higher number, you're

going to have a higher retention of your data.

That means that you're going to use more disc space

because you are retaining more data.

If you do a lower number,

you're going to have less data retained on discs.

So less of a need of disc space

but if you're consumers are down or not consuming

for too long, for whatever reason, then they can miss data.

So my recommendation, is of course

to maybe increase the setting and increase the disc space

because today disc is quite cheap

but losing data with your consumers is really expensive.

The other parameters you can look at that have an impact

on this are log retention millisecond

and log retention minutes.

And the smaller unit you set has a precedence

over all the other settings.

The other settings that you have in conjunction

with log retention hours is log retention bytes.

And this is the maximum amount

of bytes for each partition in your topic.

By default, it is minus one

which means you have an infinite space retention

for your topics, but it's useful to set

if you want to keep the size

of your topic log under a specific threshold.

So here is an example of the topics.

We have multiple segments and the data is written

to the active new segments, and old segments

of your time are going to be expired, deleted, well

because they are too old or your segment get too big.

So two very common pair

of options for the log cleaner policy deletes.

The first one is to set one week of retention

which is the default, which is log retention

hours equals 168 and log retention bytes equals minus one.

The second ones is infinite time retention, for example

bounded by 500 megabytes of data per topic partition.

In which case you will set log retention

millisecond equals minus one, and log retention

bytes equals this big number that represents 500 megabytes.

So hopefully that's helpful and you can set the

settings using the Kafka configs command

I showed you before.

So that's it.

I will see you in the next lecture.
