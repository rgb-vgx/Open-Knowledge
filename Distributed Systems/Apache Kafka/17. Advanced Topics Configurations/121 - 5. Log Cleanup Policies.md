Hi, this is Stephane from Conduktor

and welcome to this section

on understanding log cleanup policies.

So we've seen that Kafka topics have data that expire

and according to your policy.

And the concept of making data expire is called log cleanup.

So we have two policies in Kafka we will have a look at,

the first one is log cleanup policy equals delete,

which is the default for all the user's topics.

And it's when we delete data based on the age of the data,

and by default it is a week.

So what I've told you before is that, hey, after a week

your data in Kafka is gone.

This is because of this log cleanup policy, deletes.

You can also delete your logs

based on the max size of a log,

we'll see this in the next lecture.

By default it's minus one,

that means that the size can be infinite

but you only have one week of data.

The second one is log cleanup policy compact,

and that is actually the default

for a specific internal Kafka topic

named the consumer offsets topic.

So if we have a Kafka topics, describe commands.

So I'm going to set up a bootstrap server

to be local nine zero nine two.

I'm going to describe and then the topic

is going to be the consumer offsets topic.

So as we can see we have a lot of partitions,

but more importantly what I wanna show you

is under conflicts right here,

we have compression type equals producer

and cleanup policy equals compact.

So this is showing you right here

that this specific topic has a different cleanup policy

and we'll understand why in a few lectures.

So with a log cleanup compacts policy,

that means that you're going to be deleting your messages

based on the latest occurrence of a key.

And that means that all the keys

that you'll find before that key are going to be deleted

after the active segment is committed.

This gives you infinite time and space retention

and some interesting properties.

Now I'm going to have a full lecture

and a hands-on on compaction of the logs.

So don't worry too much right now.

So why do you need even log cleanup, and when?

When you delete data from Kafka

it allows you to control the size of all the data

that is going through Kafka on the disc

and to delete data that is supposed to have been consumed

and so therefore that is obsolete.

Also, it helps you limit the maintenance work

on the Kafka Cluster because the disc space

is going to be somewhat constant

and so therefore nothing to worry about.

So how often does log on cleanup happen?

Well, it happens whenever you have

partition segments being created.

And so if you have smaller, more segments,

that means that log cleanup is going to happen more often.

And though it happens more often

it shouldn't happen too often

because while log cleanup takes CPU and RAM-ing sources

to figure out what to delete.

So the cleaner, there's a setting

called log cleaner backoff milliseconds.

And it's mean that the cleaner is going to be checked,

checking for work every 15 seconds.

So now that we know that log cleanup is happening

I will see you in the next lecture

to understand exactly how it works.
