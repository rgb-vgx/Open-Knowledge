So one of the first used case of Apache Kafka

was to do logging and metrics application.

So all these applications were sent into Kafka.

And then in this kind of deployment

we usually want like high throughput

and you have less restriction regarding data loss

or replication of data.

And so this is why you used to have like X equals zero

and lower application factor and so on.

So then from the logging metrics aggregation

then you're going to send your data

into your favorite logging solutions, such as Splunk

CloudWatch, the ELK stack and so on.

So the architecture is simple, it looks like this.

You have Kafka in the middle

and you will create, for example,

topics for your application logs

and other topics for your metrics.

Then your application would have some kind of log forwarder

which is a producer that will send all the logs

into Apache Kafka.

And so this is a very high throughput type of producer.

And then you will also have metrics collectors.

This is also some kind of producer

that will be sending all the metrics data

into your application metrics topic.

From there you would have, for example, Splunk

and you will use Kafka Connect Sink to send data

from the logs topic and the metrics topic into Splunk

a very simple architecture, but you have to see it once.

Okay, I hope that was helpful.

And I will see you in the next lecture.
